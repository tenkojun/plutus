# 변경 이력

이 프로젝트는 [시맨틱 버저닝](https://semver.org/lang/ko/)을 따릅니다.
버전 단일 소스는 [`version.py`](version.py) 입니다.

---

## [5.3.0] — 2026-08-24

사이트가 생겼다. 그리고 캔들 위에서 휠이 안 먹던 환경을 고쳤다.

### 언어별 GitHub Pages
`https://tenkojun.github.io/plutus/` — 한국어가 메인이고 `/en/` · `/ja/` ·
`/zh-cn/` 가 나란히 선다.

**본문은 저장소에 없다.** 사이트용으로 같은 글을 한 벌 더 두면 반드시
한쪽만 고쳐지고, 어느 쪽이 최신인지 아무도 모르게 된다. 그래서 README 를
단일 소스로 두고 `tools/build_pages.py` 가 빌드 시점에 조립한다 —
README 를 고치면 사이트가 따라온다.

조립하면서 세 가지를 고친다. 이미지 경로(`webapp/static/` → `/assets/`),
저장소 안 파일로 가는 링크(사이트엔 그 파일이 없으니 GitHub 로 보낸다),
문서 맨 위 언어 전환줄(레이아웃이 스위처를 그리므로 뺀다). 상대링크가
하나라도 남으면 **빌드를 실패시킨다** — 사이트에서만 404 로 드러나는
종류라 여기서 막아야 한다.

- `site/_layouts/page.html` — 앱과 같은 팔레트, 다크/라이트 자동
- `site/_config.yml` — `jekyll-seo-tag`(og·twitter·canonical) ·
  `jekyll-sitemap`
- `.github/workflows/pages.yml` — README 나 `site/` 가 바뀔 때만 돈다

### 찾을 수 있게
- **hreflang 4개 + `x-default`.** 이게 없으면 네 언어판이 중복으로
  취급돼 하나만 색인되고 나머지는 버려진다. `x-default` 는 한국어
- `SoftwareApplication` JSON-LD — 오픈소스 · Python · MIT 라는 맥락이
  검색결과에 붙는다
- `robots.txt` → `sitemap.xml`
- 저장소 설명 · 홈페이지 · 토픽 (지금까지 전부 비어 있었다)

### 캔들 위 휠 확대
차트 위에 커서를 올리고 휠을 굴려도 아무 일도 일어나지 않는 환경이
있었다. 오버레이도, 라이브러리 옵션도, 경합하는 리스너도 아니었다 —
**WebView2 에서 휠 이벤트가 캔버스까지 안 갔다.**

컨테이너에 폴백 리스너를 단다. 차트가 이미 처리했으면(`defaultPrevented`)
**손대지 않는다** — 안 그러면 잘 되는 환경에서 확대가 두 배로 걸린다.
커서 위치를 기준점으로 잡아 그 지점이 제자리에 남게 확대한다.
`deltaMode`(줄·페이지 단위 휠)도 환산한다.

측정: 정상 경로 ×0.909(한 번만 확대됨) · 막힌 경로 ×0.698 확대,
599 → 858 축소.

---

## [5.2.0] — 2026-08-23

보고서도 4개 언어로 나온다. 5.1.0 은 화면만 바꿨고, 분석 보고서와 용어집
66개는 한국어로 남아 있었다. 이번에 그걸 끝냈다.

### 왜 화면과 같은 방식이 안 통했나
보고서는 f-string 으로 HTML 을 조립한다.

    f'<div class="cs2">확률 {v.direction_prob:.1%} · CI [{lo}, {hi}]</div>'

출력은 종목마다 다르므로 완성된 문장을 키로 쓸 수 없다. 그래서 **f-string
의 리터럴 조각**('확률 ')을 키로 쓰고, 렌더가 끝난 문자열을 한 번 통과
시킨다. 키는 AST 로 뽑는다 — 손으로 모으면 반드시 빠뜨린다.

- `engine/jiqtx/i18n.py` — 태그 밖 텍스트와 툴팁 속성만 치환.
  `<style>`·`<script>`·class·id 는 건드리지 않는다
- `engine/jiqtx/locale/{en,ja,zh_cn}/` — 언어당 **2,235개**
- 진입점 6개에 `lang` (전문 HTML · 간단 리서치 · 포트폴리오 · 마크다운).
  기본값 `ko` 라 기존 호출은 그대로다
- 설정 → 화면에 **보고서 언어**. 테마와 성질이 같다 — 자기완결 HTML 이라
  **생성 시점에 굳는다.** 이미 만든 보고서는 다시 생성해야 바뀐다

### 실측
보고서 8종(88,861자) 기준 en · ja · zh-CN 모두 **99.0%**, CLI 마크다운은
**99.9%**. 남은 882자는 일부러 남긴 맨 조사다 — 조사는 혼자서 뜻이 없어
사전에 넣지 않는다.

### 조용히 틀리던 것 넷
조각 치환은 "안 맞는" 게 아니라 "짧은 키가 대신 맞는" 형태로 틀린다.
실제 보고서를 눈으로 훑다가 한국어 문장 한가운데 영어 단어 하나만 박힌
곳들을 발견했고, 원인이 넷이었다.

1. **경계를 그룹 전체에 걸었다.** 문장부호로 끝나는 키가 뒤에 한글이
   온다는 이유로 거부되고, 정규식이 되돌아가 더 짧은 키를 먹었다.
   경계는 키마다, 그 끝이 한글일 때만 건다
2. **본문의 `<` 를 태그로 오인.** `|t| < 2 인 지평의 …` 에서 뒤 문장이
   통째로 번역 대상에서 빠졌다. `<` 뒤에 글자·/·!·? 가 와야 태그로 본다
3. **HTML 이스케이프.** 따옴표가 든 문장은 출력에서 `&#x27;` 로 바뀌어
   있어 원문 키로는 영영 안 맞는다. 이스케이프 형태를 별칭으로 등록
4. **%-포맷 리터럴.** `하위 %.0f%% 경계값` 은 출력에서 `하위 5% 경계값`
   이다. 추출기가 포맷 자리에서 자르게 했다

되돌린 것도 있다. 1번을 고치자 맨 조사('이 ')가 문장 한가운데서 발동해
"이 선택 하나가" 에서 지시어가 지워졌다. 사전에서 뺐다 — 조사가 한국어로
남는 편이 단어가 사라지는 것보다 낫다.

### 저장소에 안 들어가 있던 사전
`.gitignore` 의 `_*.py`(작업용 임시 스크립트 규칙)에 사전 파트가 전부
걸렸다. `git status` 는 깨끗한데 저장소에는 사전이 없었고, `catalog()` 는
예외를 삼키므로 클론한 쪽에서는 보고서가 그냥 한국어로 나왔을 것이다.
예외 규칙을 넣고, `git ls-files` 로 확인하는 테스트를 넣었다.

### 도구
- `tools/i18n_extract.py` — AST 추출 (HTML·포맷 자리에서 쪼갬)
- `tools/i18n_coverage.py` — 실제 보고서에 한글이 몇 자 남는지 실측
- `tools/i18n_rank.py` — 등장횟수 × 길이로 우선순위
- `tools/i18n_todo.py` · `tools/i18n_register.py`

### 테스트
268개. 보고서 번역 검사 15종 — 사전이 git 에 있는가, `lang='ko'` 가
바이트 단위 무동작인가, 경계 규칙이 '생존편향된' 을 안 건드리는가,
CSS/JS 를 안 건드리는가, 세 언어가 같은 키를 갖는가, 중복·오타 키,
진입점의 lang 기본값, 그리고 앱에서 실제로 고를 수 있는가.

마지막 것도 실제로 잡았다: 테스트만 `.lower()` 를 빼먹어 `zh_CN` 을
찾았고, import 실패는 skip 이라 **중국어 검사 3종이 통째로 꺼져 있었다.**

---

## [5.1.0] — 2026-08-23

문을 열었다. 한국어를 못 읽는 사람이 이 프로그램 앞에서 할 수 있는 일이
없었고, 가입해도 내가 승인할 때까지 아무것도 못 봤다.

### 회원가입 즉시 사용
가입하면 바로 쓴다. 승인 대기를 없앴다 — 처음 온 사람이 아무것도 못 해
보고 떠나는 게 유일한 효과였다. 무료 등급(보고서 3회/일)으로 시작하고,
한도는 서버가 막으므로 문까지 잠글 이유가 없다.

- Worker 가입 INSERT 를 `'pending'` → `'active'`
- 로그인의 pending 게이트는 남긴다 — 관리자가 정지시킬 수단이다
- `0004_auto_approve.sql` — 승인만 기다리던 기존 계정을 열어 준다
- 실제 Worker 에 가입 → 즉시 로그인 → `/auth/me` 까지 확인 (tier=free)

### 화면 언어 — 영어 · 日本語 · 简体中文
설정 → 화면에서 고른다. 언어당 401개 문자열.

구현은 **한국어 원문을 키로 쓰는 런타임 치환**이다. 11,000줄 단일 HTML 의
요소마다 키를 박으려면 사실상 전 파일을 고쳐야 하고 그 과정에서 조용히
깨지는 곳이 반드시 생긴다. 사전에 있는 문자열만 바꾸므로 종목명·뉴스
제목·사용자 입력은 건드리지 않고, 사전에 없으면 한국어로 남는다.

- MutationObserver 로 나중에 그려진 위젯도 같은 언어로. `characterData`
  는 보지 않는다 — 시세 숫자가 초당 여러 번 바뀐다
- 날짜·시각도 화면 언어를 따라간다(`_LOC()`). 시계만 "12시 43분 8초" 로
  남으면 설정이 안 먹은 것처럼 보인다. `'ko-KR'` 하드코딩 14곳을 걷어냈다
- **분석 보고서와 용어집 66개는 한국어 그대로다.** 화면만 바뀐다

네 언어를 실제로 띄워 확인했다. 영어/일본어/중국어에서 화면에 남은
한국어는 개발자 이름(고유명사) 하나뿐이고, 한국어로 되돌리면 이전과
완전히 같다.

한 번 틀렸던 곳: 치환 중에 들어온 DOM 변경을 `busy` 플래그로 버리고
있었다. 부팅 중 첫 치환과 위젯 렌더가 겹치면 그 위젯만 한국어로 남았다.
`apply()` 는 텍스트 값과 속성만 건드려 childList 변경을 스스로 만들지
않으므로 그 가드는 애초에 필요 없었다.

### README 다국어
`docs/README.en.md` · `README.ja.md` · `README.zh-CN.md` — 요약이 아니라
전체 번역이다. 섹션·표·mermaid·접힘 블록까지 옮겼고 네 문서 상단에 언어
전환줄을 뒀다. 겸사겸사 오래된 숫자(라우트 116 → 125, jiqtx 32 모듈 →
33)와 EXE 빌드 안내를 고쳤다.

### 테스트
253개 (246 + `tests/test_i18n.py` 7개). 사전이 세 언어에서 같은 키를
갖는지, 번역값에 한글이 남았는지, 중복 키가 앞의 것을 덮지 않는지,
`'ko-KR'` 하드코딩이 되살아나지 않는지를 본다 — 전부 직접 그 언어로
띄워 보기 전에는 안 보이는 것들이다.

---

## [5.0.0] — 2026-08-21

v4.0.0 이후 이어진 전면 감사의 마무리. **엔진의 모든 모듈에 진실값 대조가
걸렸고**, CI 가 매 푸시마다 246개 테스트를 돌린다.

### 이번 회차
**자산군별 연율화** — CLAUDE.md 가 명시하지만 검증이 없던 부분이다.
24/7 시장은 √365, 거래일 시장은 √252 여야 한다. 연율 60% 를 주입해
확인했다.

    자산        인덱스    ann    GARCH 장기    오차
    암호자산    달력일    365      58.4%      2.7%
    주식        거래일    252      58.4%      2.7%

`√252` 를 잘못 쓰면 **48.5%** 로 20% 과소평가된다. 연율화를 틀리는 것은
이 저장소에서 이미 두 번 난 버그 계열이라(드리프트 SE · ES 부호) 마지막으로
확인했다 — 정상이었다.

**`PLUTUS_DATA_DIR`** — 데이터 폴더를 지정할 수 있게 했다. 기본 동작은
그대로다(변수를 안 주면 앱 폴더 옆 `.data`). 넣은 이유는 테스트 격리다.
지정할 방법 자체가 없으면 격리는 언제나 우회로가 된다. 지정했는데 쓸 수
없으면 조용히 다른 곳에 쓰지 않고 알린다.

### v4.x 전체에서 잡은 수치 버그 넷
전부 **파이프라인을 실제로 돌려야** 드러나는 종류였다. 단위 테스트나
코드 리뷰로는 잡히지 않는다.

| 버전 | 버그 | 영향 |
|---|---|---|
| 4.2.0 | ES 부호 (좌측 꼬리 변환) | 꼬리 손실 **1/3** 과소평가 |
| 4.3.0 | 칼만 프로세스 노이즈 | 베타 추적 오차 **78배** → 미달헤지 |
| 4.4.0 | 드리프트 SE 연율화 누락 | t값 **16배** → 전 지평 허위 유의 |
| 4.6.0 | IV 차익거래 하한 | **스마일 풋 윙 소실** → 스큐·RND 꼬리 |

넷 다 공교롭게 **"실제보다 안전하거나 확실해 보이게"** 만드는 방향이었다.

### 검증 완료 — 정상이던 것
- 추정량: EDGE 스프레드 · GARCH 모수 · Murphy 판별비 411배 ·
  conformal 커버리지 89.9% · 켈리 단조성
- 시뮬레이션: 1일 σ 가 GARCH 와 비율 1.000 · √t 척도 · CVaR ≥ VaR ·
  시드 재현성
- 블랙숄즈: 풋-콜 패리티 오차 **1.78e-14** · 그릭스가 수치미분과 1e-7 이내
- RND: 로그정규 복원 평균 오차 **0.02%** · 밀도 적분 1.000000
- 포트폴리오: 리스크패리티 기여도 균등 · 최소분산 우위 · 공분산 PSD
- 판정 엔진: 분열 시 구간이 4.6배 넓어짐 · 극단값 하나에 +0.045 만 이동
- 거시보드: `|t|<2` 면 "유리/불리" 라는 말 자체를 안 씀
- 쿼터: 스레드 40개 동시 요청에도 한도 3건 정확히 유지

### 남은 것 — 정직하게
- **CSP `unsafe-inline`** — UI 11,000줄에 인라인 스타일 751곳. nonce 로
  바꾸려면 UI 재작성이 필요하다. 회귀 위험이 실재해 별도 회차로 잡아야 한다
- **코드 서명** — 비용 문제. 없으면 SmartScreen 경고가 남는다
- **`except Exception` 35곳** — 전역 처리기는 넣었지만 개별로 삼키는
  자리는 그대로다. 하나씩 봐야 하는 작업이다
- **성능 프로파일링** — `analyze()` 가 합성 데이터로 17초. 측정만 했고
  개선은 안 했다

---

## [4.8.0] — 2026-08-21

### 추가 — 쿼터 동시성 검증
CLAUDE.md 가 기록한 실제 사고 둘을 테스트로 막는다.

- 스레드 **40개를 같은 순간에** 출발시켜도 무료 한도 3건만 통과한다.
  `BEGIN IMMEDIATE` 가 빠지면 여기서 넘어간다
- `user_id` 가 `None` 이면 거부한다. NULL 키로 쓰면 SQLite 가 PK 중복을
  허용해 카운트가 누적되지 않는다 — 실제로 한도가 통째로 우회됐다
- 플래티넘 무제한 · `used + remaining ≤ limit` 정합성

### 고침 — 테스트 격리가 실제로는 안 걸려 있었다
`conftest` 가 `PLUTUS_DATA_DIR` 을 세팅해 두고 격리됐다고 믿었는데
**아무 효과가 없었다.** `engine/paths.py` 는 그 변수를 읽지 않고
`LOCALAPPDATA` 만 보며, `AUTH_DB` 는 **import 시점에 상수로 확정**된다.

그래서 쿼터 테스트가 **실제 `.data/auth.db` 에 행을 쓰고 있었다.**
전체 실행에서만 실패하던 것도 이 때문이다 — 실행마다 실제 DB 에 상태가
쌓여 다음 실행을 오염시켰다.

지금은 값을 확정한 모듈들의 속성을 직접 갈아 끼운다. `_conn()` 이 호출
시점에 모듈 전역을 읽으므로 import 이후에도 먹는다. **격리가 실제로
걸렸는지 확인하는 테스트**도 함께 넣었다 — 조용히 실패하는 격리는 없는
것만 못하다.

실제 DB 에 남아 있던 테스트 흔적(uid 424242 · 700001~4 · 999001)을
지웠다. 실제 사용자 데이터는 그대로다.

테스트 241개.

---

## [4.7.0] — 2026-08-21

엔진의 마지막 미검증 영역 셋. **이번에는 버그가 없었다** — 정상이라는
사실을 테스트로 고정했다.

### 판정 엔진 — 로그오즈 풀링
- 의견이 모이면 그쪽으로 (0.70 / 0.30 / 0.50 그대로 추종)
- 의견이 갈리면 0.5 로 축소하고 **구간을 넓힌다.** 단순 평균도 0.5 를
  주지만 그건 "확신 있는 0.5" 로 읽힌다 — **정보는 구간 폭에 담긴다.**
  합의 시 폭 0.16 vs 분열 시 **0.74**
- 극단 의견 하나(0.99)를 추가해도 **+0.045** 만 움직인다. 시끄러운
  에이전트가 전체를 끌고 가지 못한다
- 거부권 → 비중 0 강제 · 확률과 구간은 [0,1] 안

### 거시보드 — `|t| < 2` 규칙
- 유의하지 않으면 라벨 "중립", 코멘트는 *"통계적으로 0과 구별되지
  않습니다"* — **"유리/불리" 라는 말 자체를 쓰지 않는다**
- 영향은 손으로 쓰지 않고 **베타 부호 × 변화 방향**으로 정해진다
  (4가지 조합 전부 확인)
- 경계 `|t| = 2.00` 처리 · NaN/None → "판단보류"

### RND — Breeden-Litzenberger
알려진 로그정규(σ=28%, T=60일)에서 합성 옵션 체인을 만들어 주입했다.
`yfinance` 를 갈아 끼우므로 네트워크를 타지 않는다.

    항목         RND        이론      오차
    평균      100.6348   100.6597    0.02%
    중앙값     99.8953   100.0132    0.12%
    표준편차    11.4117    11.4642    0.46%
    q05        82.8821    82.9777    0.12%
    q95       120.3485   120.5461    0.16%

밀도 적분 **1.000000** · 음수 0개 · CDF 단조 · 분위수 순서 정상.

테스트 235개.

---

## [4.6.0] — 2026-08-21

### 고침 — 내재변동성의 차익거래 하한이 틀려 스마일의 풋 윙이 빠졌다 ★★
**네 번째 수치 버그**다. 옵션은 해석해가 있으므로 "대충 맞는 것 같다" 로
넘어갈 이유가 없어서 항등식과 대조했다.

`implied_vol` 의 하한 검사

    intrinsic = max(K-S, 0)                       # 할인 안 된 내재가치
    if price < intrinsic · e^(-rT) · 0.99: NaN    # 그걸 통째로 할인

유러피언 옵션의 차익거래 하한은 그게 아니다.

    콜:  S·e^(-qT) - K·e^(-rT)
    풋:  K·e^(-rT) - S·e^(-qT)

풋에서는 잘못된 하한이 **실제 BS 가격보다 높게** 나온다.

    K     T    BS가격   옛 하한   올바른 하한    판정
    115  3.0    9.62    13.17      4.95       거부(NaN)
    130  1.0   26.33    28.54     25.90       거부(NaN)
    115  1.0   12.21    14.27     11.49       거부(NaN)

멀쩡한 가격을 차익거래 위반으로 보고 NaN 을 냈다. 그 결과 **깊은 내가격
풋이 스마일에서 통째로 빠졌다** — 하필 **스큐를 담고 있는 구간**이고
RND 의 좌측 꼬리가 거기서 나온다. 꼬리 위험을 보겠다는 도구가 꼬리 정보를
잃고 있었다.

콜 쪽은 반대로 하한이 너무 낮아 차익거래를 위반하는 가격도 통과시켰다.
상한(콜 ≤ S·e^(-qT), 풋 ≤ K·e^(-rT))도 함께 넣었다.

수정 후 IV 왕복 실패 **9건 → 2건**. 남은 2건은 시간가치 ≈ 0.3,
vega ≈ 0.00001 로 IV 자체가 식별되지 않는 구간이고, 스마일 빌더가
`0.01 < iv < 4.0` 으로 거른다. 없는 값을 지어내는 것보다 NaN 이 낫다.

### 검증했고 정상이던 것
**블랙숄즈**
- 풋-콜 패리티 `C - P = S·e^(-qT) - K·e^(-rT)` — 45개 조합 최대 오차
  **1.78e-14**
- 그릭스가 수치미분과 1e-7 이내 (delta · gamma · vega)
- 콜 델타 [0.051, 0.979], 행사가에 대해 단조 감소

**포트폴리오**
- 비중 합 1 · 롱온리 (동일 · 역변동성 · 최소분산 · 리스크패리티)
- 리스크 패리티가 실제로 기여도를 균등화한다 (상대 기여 σ < 0.02)
- 최소분산이 동일가중보다 낮은 분산
- 공분산이 PSD 이고 대칭

테스트 208개.

---

## [4.5.0] — 2026-08-21

스트레스 · ML 을 진실값 주입 방식으로 훑었다. 이번에는 계산 오류가 아니라
**안전 규약의 불일치**와 **문서와 동작의 어긋남**이 나왔다.

### 굳힘 — 같은 필드에 반대 기본값을 쓰고 있었다
    agents.py:109   ok7  = bool(st.get("within_limit", False))   fail-closed
    agents.py:277   veto = not st.get("within_limit", True)      fail-open

키가 빠지는 순간 게이트는 막는데 리스크 책임자는 통과시키는 모순이 된다.
스트레스를 못 재는 포지션에 사이즈를 실을 이유가 없으므로 **fail-closed**
로 통일했다.

확인한 안전 속성 — `agents.py:418-420` 에서 `vetoes` 나
`"sizing" in disabled` 면 비중이 0 으로 강제된다. G7 실패가 실제로
SIZE_ZERO 로 이어진다. 매크로가 없어 스트레스를 아예 못 재는 합성 실행에서도
최종 비중은 0 이었다.

### 고침 — 트리플 배리어 주석이 실제 동작과 달랐다
`label: +1 / -1 / 0` 이라 3클래스로 읽히는데, 수직배리어(시간 만료)에
걸려도 0 이 아니라 **비용 차감 후 수익률의 부호**로 라벨한다. 0 은 수익률이
정확히 0 일 때뿐이라 측도 0 이다 — 배리어를 **32σ(≈38%)** 까지 넓혀도
0 라벨은 마지막 관측 하나뿐이었다.

버그가 아니라 López de Prado 의 두 변형 중 이진 쪽을 고른 것이다. 다만
다운스트림의 기저율·정확도 해석이 이 가정 위에 서 있어서, 주석을 실제와
맞추고 테스트로 못 박았다.

### 검증했고 정상이던 것
**트리플 배리어**
- 방향: 상승 경로 +1:1051 / -1:148 · 하락 경로 +1:132 / -1:1067
- 무추세에서 대칭 (비 1.18)
- 비용을 넣으면 +1 이 준다 (1051 → 982) — "비용 빼면 손해였을 움직임" 이
  -1 로 가는 게 요점이다
- 도달 시점이 지평을 넘지 않는다 (미래 훔쳐보기 없음)

**스트레스 부분베타**
복합 시나리오에서 단변량을 그대로 더하면 같은 시장 충격을 여러 번 센다.
부분(다변량) 베타를 쓰면 덜 극단적이 되는 것을 확인했다 —
단변량 -11.93 vs 부분베타 -8.40.

테스트 148개.

---

## [4.4.0] — 2026-08-21

### 고침 — 다지평 드리프트 t값이 16배 부풀어 있었다 ★★
시뮬레이션·다지평을 진실값 주입 방식으로 훑다가 나왔다. **세 번째 수치
버그**다(ES 부호 · 칼만 튜닝에 이어).

`engine/jiqtx/horizons.py`

    se_ann = sd · √ann / √n        ← 분자만 연율화, 분모의 T 는 일 단위

연율화된 평균의 표준오차는 `σ_ann / √T_years` 이고 `T_years = n/252` 이므로
`sd·ann/√n` 이어야 한다. 정확히 **√252 ≈ 15.9 배** 차이다.

실측 (장기 상승 + 최근 1년 하락 합성 계열)

    지평     n     μ̂(연율)   t(고치기 전)   t(올바름)
    단기    63     16.5%        5.66         0.36
    중기   252     -5.5%       -3.53        -0.22
    장기  1252     16.6%       31.96         2.01

**세 지평 모두 `|t|≥2`** 로 "드리프트가 통계적으로 유의하다" 고 나왔다.
실제로는 장기만 겨우 2.01 이고 단기는 0.36 이다.

63일 표본에서 드리프트가 유의하다고 말하는 것은 `σ/√T` 를 무시한다는
뜻이다. *"유의하지 않은 값으로 서사를 만들지 않는다"* 는 이 엔진의 전제를
정면으로 어기는 버그였다. 수정 후 단기·중기는 "아니오", 장기만 "예" 로
나온다.

### 검증했고 정상이던 것 — 시뮬레이션 엔진
- 1일 지평 σ 가 GARCH 현재 변동성과 **비율 1.000** 으로 일치
- 252일로 늘리면 √t 척도 (실제/예상 1.024)
- CVaR ≥ VaR, 종착분포 직접 계산과 소수점까지 동일
- 분위수 순서 · 확률 범위 · 시드 재현성
- `prob_up` 0.553 이 순진한 GBM 0.615 보다 보수적 — 의도대로다

### 검증했고 정상이던 것 — 다지평 패널
장기 +127.9% 와 중기 -5.3% 를 갈라 보고, 이렇게 지적했다.

> 샤프 부호가 지평 간 뒤집힌다 — 단기 +0.72 · 중기 -0.22 · 장기 +0.90.
> 어느 하나만 인용하면 정반대 결론이 나온다.

지평을 합치지 않고 **어긋나는 지점을 드러내는 것**이 목적이라는 설계가
그대로 작동한다.

테스트 143개 (시뮬 4 · 다지평 4 추가).

---

## [4.3.0] — 2026-08-21

ES 부호 오류(v4.2.0)를 잡은 것과 **같은 방식**으로 나머지 모델을 훑었다.
전부 진실값을 아는 합성 데이터에 넣고 되찾는지 확인했다.

### 고침 — 칼만 시변 베타가 롤링 창보다 못했다
베타가 0.5 → 1.6 으로 계단 변화하는 계열에서

    전환 후 750일(3년)이 지나도  1.44   (진짜 1.60, 오차 0.156)
    같은 함수의 롤링 252일 창은   1.62   (정확)

개선하려고 넣은 칼만이 단순 롤링보다 못한 상태였다. `beta_now` 는 헤지
사이징에 쓰이므로 10% 과소평가가 그대로 미달헤지가 된다.

편향이 아니라 **속도** 문제였다 — 궤적이 단조 상승 중이었고 프로세스
노이즈 `q` 가 너무 작았다. 반응성과 매끄러움을 함께 재서 골랐다.

    q       반감기   최종β(진짜1.60)   잡음(고정베타 σ)
    1e-6     611일        1.113          0.005
    1e-5     246일        1.444          0.008   ← 옛 기본값
    5e-5     115일        1.602          0.014   ← 지금
    1e-3      26일        1.627          0.042

`5e-5` 는 추적 오차를 **78배** 줄이면서(0.156→0.002) 잡음은 1.4%p 만
늘고, 반감기가 롤링 252일 창의 구조적 반감기(~126일)와 맞는다.

### 추가 — 모델별 진실값 복원 테스트 10개
- 팩터 베타 복원 (1.150→1.151 · -0.400→-0.370 · 0.250→0.225)
- 진짜 노출은 |t|≥2, 순수 노이즈는 |t|<2
- **없는 알파를 만들지 않는가** (알파 0 주입 → t=1.42, 중립 판정)
- 칼만이 변화를 따라가는가 / 고정 베타에서 출렁이지 않는가
- 레짐이 저→고→저 변동성 전환에서 양 끝을 같은 상태로 보는가
- GPD 가 팻테일을 팻테일로 보는가 (t(4) 이론 ξ=1/ν=0.25 를 감싸는지)
- 드리프트 축소가 실제로 걸리는가

### 검증했지만 고치지 않은 것
정상인데 의심스러워 보이는 것들이라, **다음에 또 의심하지 않도록**
테스트로 고정해 두었다.

- `drift_posterior.ci95` 는 사후분포가 아니라 **μ̂ 기준**이다. 보고서가
  `μ̂ → SE(μ̂) → 95% 구간 → 사후 드리프트` 순으로 보여 주면서 "추정치가
  자기 표준오차에 묻힌다" 를 드러내는 게 그 절의 논지다. 사후 구간으로
  바꾸면 그 논지가 사라진다
- Elastic-Net 이 노이즈 팩터를 선택 목록에 남기는 것. 사후에 유의하지
  않은 회귀변수를 빼면 그게 또 다른 선택 편의를 만들고, 보고서가 계수
  옆에 t값을 함께 보여 주므로 읽는 쪽이 판단할 수 있다

테스트 135개.

---

## [4.2.0] — 2026-08-21

### 고침 — Expected Shortfall 이 꼬리 손실을 3분의 1로 과소평가했다 ★★
파이프라인을 합성 데이터로 **처음 end-to-end 돌려 보다가** 잡았다.
산출물에 수학적으로 불가능한 값이 있었다.

    var_fhs_evt = 0.021841
    es_fhs_evt  = 0.016556      ← ES < VaR

ES(조건부 기대손실)는 VaR 너머 손실의 **평균**이라 정의상 VaR 보다 작을
수 없다.

McNeil-Frey 의 GPD ES 는 **우측 꼬리** 공식이다. 이 엔진은 손실(좌측)을
보므로 `Y = -z` 로 뒤집어 유도한 뒤 되돌려야 하는데, 되돌릴 때 두 번째
항의 부호와 `u` 의 부호를 함께 놓쳤다.

    올바름:  es_z = zq/(1-ξ) - (β + ξ·u)/(1-ξ)
    기존:    es_z = zq/(1-ξ) + (β - ξ·u)/(1-ξ)

몬테카를로 대조 (표본 20만)

    분포      alpha    VaR   ES(기존)  ES(수정)  ES(진실)
    t(4)       0.05   1.500    0.734    2.268    2.268
    t(4)       0.01   2.659    2.201    3.734    3.736
    t(6)       0.05   1.583    0.951    2.214    2.214
    정규        0.05   1.647    1.234    2.061    2.061

t(4) 팻테일에서 진짜 2.27 을 **0.73** 으로 냈다. 리스크 시스템에서 꼬리
손실을 과소평가하는 것은 가장 위험한 방향의 오류다 — 실제보다 안전해
보이게 만든다. 수정 후 진실값과 오차 0.0~0.2%.

ξ≥1 이면 평균이 존재하지 않는다. 없는 값을 지어내지 않고 NaN 을 낸다 —
게이트 철학과 같다.

> `es_historical` 은 처음부터 정상이었다(0.0230 → 0.0280). 영향은
> FHS-EVT 경로에 한정된다.

### 고침 — OHLCV 컬럼 규약이 경로마다 달랐다
`CLAUDE.md` 는 "소문자 계약" 이라고 적혀 있는데 `engine/jiqtx/` 내부는
대문자를 요구했다. 운영에서는 jiqtx 가 자기 수집 경로(yfinance, 대문자)를
써서 드러나지 않았지만, `analyze()` 가 **공식 지원하는** df 주입
(백테스트·오프라인 검증)으로는 문서를 따르는 순간 `KeyError` 로 죽었다.

어느 쪽이 옳은지 다투는 대신 경계에서 받아 준다(`_normalize_ohlcv`).
문서도 실제와 맞게 고쳤다.

### 추가 — 파이프라인 end-to-end 테스트 15개
개별 추정량은 이미 검증돼 있었다. 하지만 **부분이 다 맞아도 조립이 틀리면
사용자가 보는 값은 틀린다.** 위 ES 버그가 정확히 그런 경우다 — 추정량
테스트로는 안 잡히고, 전 구간을 돌려야 드러난다.

- **감사 가능성** — 같은 입력이면 같은 판정. 시드가 새면 어제와 오늘의
  판정이 달라지고, 그러면 왜 그렇게 나왔는지 아무도 설명할 수 없다
- **신호 없는 데이터에 신호를 만들지 않는가** — 순수 GBM 에 대해 방향
  확률 49.8%, 신뢰구간 [38.9%, 60.7%] 가 50% 를 포함, 비중 0%,
  `ml_probability` 무효화. 이 엔진의 핵심 주장이 실증됐다
- ES ≥ VaR · VaR 양수·1 미만 · 커버리지가 alpha 근처인가
- 최종 비중이 **모든** 상한 이하인가, 무엇이 묶었는지 밝히는가
- `High < Low` 같은 모순을 잡는가
- 사용자가 보는 숫자에 NaN 이 새지 않는가
- 연율화가 맞는가 — 넣은 22% 를 GARCH 가 되찾는가

테스트 125개.

---

## [4.1.0] — 2026-08-21

"전부 95점 넘게" 를 목표로 한 전면 강화. 배포된 버전에 있던 문제를
실측으로 찾아 고쳤고, **다시 깨지면 잡히도록 테스트로 묶었다.**

### 추가 — 테스트 84개와 CI
테스트도 CI 도 0개였다. 그래서 고친 것이 다시 깨져도 알 방법이 없었다.

- `tests/test_estimators.py` — 추정량이 합성 진실값을 복원하는가.
  `_validate.py` 에 같은 시뮬레이션이 있었지만 **숫자를 출력만** 해서
  회귀를 못 막았다. 판정 기준을 붙였다.
  EDGE 스프레드(5bp→5.4bp) · GJR-GARCH 지속성 · Murphy 판별비(411배) ·
  conformal 커버리지(팻테일·레짐전환 포함 89.9%) · 켈리의 추정오차 단조성
- `tests/test_security.py` — 라우트 128개 인증 매트릭스, Worker 의
  requireAdmin·등급 화이트리스트·PBKDF2 상한
- `tests/test_ui_static.py` — 브라우저 없이 도는 UI 검사. 인라인 JS 구문,
  테마 8종 변수 완전성, 명암비, 강조 위계, 이스케이프 단일성
- `tests/test_reliability.py` — 로그 회전, 예외 훅, 500 응답 형식
- `tests/test_regressions.py` — v3.4.2~v4.1.0 에서 잡은 버그 전부
- `.github/workflows/ci.yml` — **네트워크를 타지 않는다.** 시세 API 가
  죽어도 초록이어야 한다. Worker 문법과 마이그레이션 2회 적용도 본다

### 보안
- **무인증 라우트 16개.** 심각한 셋 — `/api/datasources/key` 는 인증 없이
  API 키를 덮어썼고, `/api/llm/auto_setup` 은 인증 없이 Ollama 설치를
  트리거했으며(다운로드+실행), `change_password` 는 소유자 토큰을 대리
  전송했다
- **기본 바인딩이 `0.0.0.0` 이었다.** 앱을 켜는 것만으로 같은 망의 모든
  기기에 열렸다. 이제 기본 `127.0.0.1` 이고, 폰으로 보기는 설정에서 켠다.
  Jupyter·TensorBoard 도 같은 이유로 기본이 localhost 다
- **CDN 스크립트를 로컬로 들여왔다.** unpkg/jsdelivr 에서 실행 시점에
  받아 왔는데, CDN 이 오염되면 임의의 JS 가 이 앱의 출처로 실행된다.
  jsdelivr 은 파일을 동적으로 재압축해서 SRI 조차 못 건다(그쪽 파일
  주석의 경고). 합쳐서 182KB — [THIRD-PARTY.md](THIRD-PARTY.md) 참조
- **보안 헤더** — CSP · nosniff · frame-ancestors · Referrer-Policy ·
  Permissions-Policy, `/api/` 응답에 no-store. CDN 을 없앤 뒤에야 CSP 가
  실질적이 됐다

### 신뢰성
- **전역 예외 처리기가 없었다.** 라우트에서 예외가 새면 Flask 기본 500
  HTML 이 나가고 서버엔 흔적이 안 남았다
- **배경 스레드 예외가 사라졌다.** 앱은 살아 있는데 기능 하나만 조용히
  멈춘 상태가 됐다. 사용자는 "가끔 안 된다" 고만 말할 수 있다
- **`app.log` 가 무한히 커졌다.** 5MB 회전 · 3개 보관

### 접근성 — 8개 테마 전부 WCAG AA
`--txt-mute` 가 전 테마 미달이었다(2.10~3.14). 색상은 유지한 채 HSL 의
L 만 옮겨 기준을 넘겼다 — 무작정 회색으로 바꾸면 테마의 성격이 죽는다.

    대비 미달   11건 → 0건 · 전체 최저 4.58
    강조 위계   txt 13~16 > dim ~7.3 > mute ~4.6

`--cyan-dim` · `--amber` · `--cyan` · `--up` 도 글자색으로 쓰여서 함께
올렸고, 그 결과 white 의 `--on-amber` 가 깨지는 것을 **테스트가 즉시
잡아** 짝을 다시 맞췄다.

### 고침 — 의존성 목록이 실제와 달랐다
`requirements.txt` 가 `torch`·`xgboost`·`statsmodels`·`arch`·`hmmlearn`·
`matplotlib` 을 요구했는데 **import 하는 파일이 0개**였고, 정작 실시간
시세에 쓰는 `websockets` 는 빠져 있었다. `arch`/`hmmlearn` 은 윈도우에서
컴파일러 없이 설치가 자주 깨져 README 의 "30초 시작" 이 첫 줄부터 막혔다.
`tools/check_requirements.py` 가 양방향으로 검사하고 CI 가 매번 돌린다.

### 고침 — dark 테마가 상승/하락 색을 정의하지 않았다
`:root`(cyber)의 쨍한 색을 상속받아, 차분한 팔레트에서 상승/하락만 튀었다.
테스트가 잡았다.

> 측정할 때는 CSS `transition` 을 꺼야 한다. 켜 둔 채로 재면 전환 중간값을
> 읽어서 멀쩡한 요소가 깨진 것처럼 보인다 — 이번에 두 번 속았다.

---

## [4.0.0] — 2026-08-20

전면 감사 후 일괄 수정. 실행 중인 앱과 소스를 실측해 모은 결함을 한 번에
처리했다. 메이저 버전을 올린 이유는 **회원 등급이 저장되는 위치가
바뀌었기** 때문이다 — 로컬에서 중앙 서버로.

### 변경 — 회원 등급 중앙화 ★
등급이 각 PC 의 `.data/auth.db` 에만 있었다. 그래서

- 다른 PC 에서 로그인하면 등급이 사라졌고
- 그 파일을 직접 고치면 **누구나 플래티넘이 됐다**

서버가 모르는 값이라 막을 방법이 없었다. 한도(3회/일) 판정은 서버에서
하는데 정작 그 한도를 정하는 등급이 로컬이라 실질적으로 우회됐다.

이제 중앙 D1 의 `user_tier` 가 유일한 진실이다. 어느 PC 에서 로그인해도
같은 등급이 따라온다.

- `POST /admin/set_tier` — 관리자만. 없는 사용자면 404, 등급 값도 검사.
  `free` 로 내리면 행을 지워 기본값으로 되돌린다. `audit_log` 에 기록
- 관리자 패널에 등급 드롭다운. 실패하면 고른 값을 되돌린다
- 로컬 표는 **중앙에 못 닿을 때만 쓰는 캐시**로 강등했다. 강등이 즉시
  반영되도록 캐시가 중앙을 이기지 못하게 했고, 등급 변경 후 신원 캐시를
  비운다(안 그러면 60초 동안 옛 등급으로 보인다)
- 다른 사용자의 등급을 물을 때 세션 값을 쓰지 않는다 — 로그인한 사람의
  등급이 남의 것으로 새어 나가면 안 된다

> 마이그레이션: `cd auth-worker && npm run db:migrate`
> 구버전 앱은 계속 동작하지만 여전히 로컬 등급을 본다.

### 보안
- **저장형 XSS** — 가입 때 넣은 닉네임이 이스케이프 없이 관리자 패널의
  `innerHTML` 로 들어갔다. 승인 대기 목록은 관리자가 반드시 여는 화면이라
  노출이 확실했다. 계정 하나만 만들면 나머지는 관리자가 대신 해 준다
- **관리자 라우트 무인증** — 로컬 프록시 3개가 저장된 소유자 토큰을 그대로
  실어 보냈다. 중앙은 `requireAdmin` 을 검사하지만 대상이 "호출한 사람"이
  아니라 "이 PC 의 주인"이다. 서버가 `0.0.0.0` 에 붙어 있으니 **같은
  와이파이의 누구나** 로그인 없이 주인의 관리자 권한을 빌려 사용자 목록을
  받고 계정을 승인·정지시킬 수 있었다. 터널을 켜면 인터넷 전체로 넓어진다
- **보고서 무인증 열람** — `/report/<파일>` 에 인증이 없어 파일명만 알면
  남의 분석 보고서를 받아 갔다
- **이스케이프 헬퍼 통합** — `esc` · `_escHtml` · `_histEsc` 셋으로
  나뉘어 있었고 그중 `_histEsc` 는 작은따옴표를 빼먹었다(40곳에서 쓰였다).
  구현을 하나로 합쳐 셋 다 같은 강도가 됐다
- **뉴스 제목** — 외부 피드에서 오는 값이 이스케이프 없이 들어갔다
- **유튜브 임베드** — 문자열로 `<iframe src="...">` 를 조립하던 것을
  `createElement` + 프로퍼티 대입으로 바꿨다. 프로퍼티 대입에는 HTML
  파싱이 없어 탈출이 성립하지 않는다. 영상 ID 형식 검사도 추가

### 고침 — 테마를 안 따라가던 색
로그인 후 화면까지 열어 재니 어두운 판이 **31종(총 50개)** 남아 있었다.
채팅·설정·게시판·업데이트·위젯독이 전부 걸렸다. 아이디·비밀번호 입력창도
흰 테마에서 검은 판이었다.

새 변수 두 쌍을 만들었다.

- `--on-cyan` / `--on-amber` — 강조색 위에 얹는 글자. 버튼 배경이
  `--cyan` 인데 글자를 박아 두면 테마가 바뀔 때 대비가 뒤집힌다.
  white 에서 실제로 어두운 배경에 어두운 글자가 됐다(명암비 1.14).
  8개 테마 전부 WCAG AA 를 넘도록 계산해 넣었다
- `--scrim` / `--scrim-soft` — 모달 뒤를 덮는 막. 검은 막을 쓰면 밝은
  테마에서 화면 전체가 어두워진다

hex 105건 · rgba 30건 · JS 인라인 3건을 치환했다.

**측정 (로그인 후 패널 10개를 연 상태, 테마 8종 순회)**

    배경 어긋남    white 20 → 0 · 나머지 7종도 0
    하드코딩 배경  83건 → 9건 (남은 건 테마 미리보기 스와치)

건드리지 않은 것 — 바꾸면 오히려 틀린다
- `themeColor('--cyan','#3df0ff')` 폴백 21곳. 변수를 읽고 실패 시 쓰는
  올바른 사용법이다
- 설정의 테마 미리보기 스와치. 각 테마 색을 그대로 보여주는 게 목적이다
- `#rep-frame` 배경. 보고서는 생성 시점에 자기 테마가 구워진 자기완결
  HTML 이라, 앱 테마를 넣으면 어두운 보고서에 흰 배경이 번쩍인다

### 고침 — 그 밖에
- **관리자 화면의 `Invalid Date`** — 중앙 서버의 `nowIso()` 는 이미 `Z`
  로 끝나는데 프론트가 `+'Z'` 를 또 붙여 `ZZ` 가 됐다. 로컬 Python 값은
  타임존이 없어 `Z` 가 필요하므로 일괄 제거가 아니라 `utcDate()` 헬퍼로
  양쪽을 처리한다. 9곳 교체
- **등급 드롭다운이 안 뜨던 문제** — `/api/admin/stats` 가 필드
  화이트리스트로 응답을 재조립하는데 `tier` 를 빠뜨렸다
- **재실행 시 "연결할 수 없습니다"** (3.4.5) — 중복 실행 감지가 한 순간의
  스냅샷이라, 옛 인스턴스가 종료되는 중에 걸리면 서버를 안 띄우고 죽은
  주소로 창을 열었다. 창을 열기 직전에 다시 확인하고, 그러고도 응답이
  없으면 창을 아예 열지 않고 이유를 알려 준다

### 도구
- `tools/check_ui.py` — 인라인 JS 구문 검사. UI 가 11,000줄 단일 HTML
  이라 편집기도 린터도 그 안의 JS 를 봐 주지 않는다. 오타 하나면 스크립트
  블록 전체가 파싱되지 않아 함수가 하나도 정의되지 않고 부팅 화면에서
  멈추는데, 콘솔을 열기 전까지 이유를 알 수 없다. 이번 작업 중 실제로
  그렇게 앱을 죽였다. 의존성 0개(Node 만)
- `tools/make_light_mark.ps1` — 밝은 배경용 마크 생성

> 측정할 때 CSS `transition` 을 꺼야 한다. 켜 둔 채로 재면 전환 중간값을
> 읽어서 멀쩡한 요소가 깨진 것처럼 보인다 — 이번에 두 번 속았고, 하마터면
> 정상 코드를 고칠 뻔했다.

---

## [3.4.5] — 2026-08-20

### 고침 — 재실행하면 "이 페이지에 연결할 수 없습니다" 가 뜨던 문제 ★
설정(테마 등)을 바꾸고 앱을 다시 켰을 때 창은 뜨는데 안에 브라우저의
`ERR_CONNECTION_REFUSED` 페이지가 나왔다.

중복 실행을 막으려고, 켤 때 8765 포트에 이미 우리 앱이 떠 있는지
`/api/health` 로 확인한다. 살아 있으면 서버를 새로 띄우지 않고 창만
연다. 문제는 그 확인이 **한 순간의 스냅샷**이라는 것이다.

    옛 인스턴스: 종료 중  ─┐
    새 인스턴스: health 확인 → 200 → "쟤가 살아있네, 서버 안 띄움"
    옛 인스턴스: 종료 완료 ─┘
    새 인스턴스: 창 열기 → 아무도 안 듣는 포트 → 연결 거부

앱을 껐다 바로 켤 때 정확히 이 창이 열린다. 설정을 바꾸고 재시작하는
흐름이 딱 그렇다.

이제 창을 열기 **직전에 다시 확인한다.** 사라졌으면 포트를 다시 고르고
서버를 직접 띄운다. 그리고 그러고도 응답이 없으면 **창을 아예 열지
않고** 왜 그런지 대화상자로 알려 준다 — 죽은 주소로 창을 여는 건 앱이
고장난 것처럼만 보이고 사용자가 할 수 있는 게 없다.

v3.4.2 에서 "조용히 죽는 경로를 없앴다" 고 했는데 이 경로가 남아
있었다. 조용히 죽지는 않았지만 **엉뚱한 화면을 보여 주고 있었다.**

---

## [3.4.4] — 2026-08-20

### 고침 — 밝은 배경에서 로고가 통째로 사라지던 문제
마크는 **흰 잉크에 알파로만** 형태를 만든다. 어두운 배경에서는 잘
보이지만 밝은 배경에서는 아무것도 안 보인다. 재어 보니 흰 배경(`#f5f7fa`)
위에서 **보이는 픽셀이 0개**였다.

두 곳에서 났다.

- **GitHub README** — 라이트 테마로 보면 로고 자리가 그냥 빈 칸이었다
- **앱 부팅 화면** — v3.3.0 에서 부팅 화면이 `var(--bg)` 를 따르게 만들면서
  같이 생긴 부작용이다. `white` 테마로 켜면 로고가 없다

앱 쪽은 `<html>` 에 `light-bg` 클래스를 달고 `invert(1)` 을 건다.
알파를 건드리지 않아 형태와 안티에일리어싱이 그대로 살아난다 — 어두운
잉크 파일을 따로 받아 올 필요가 없다.

**테마 이름으로 판정하지 않는다.** `custom` 테마는 사용자가 `--bg` 를
아무 색으로나 정할 수 있어서, `white` 만 목록에 박아 두면 흰 배경 커스텀
테마에서 똑같이 샌다. 그래서 **실제 배경색의 지각 밝기(BT.601)를 재서**
0.5 를 넘으면 밝은 배경으로 본다. 부팅 시점에는 저장된 값으로, 그 뒤
테마를 바꾸거나 커스텀 색을 편집할 때마다 다시 잰다.

작업 중 잡은 것: 정적 `filter` 규칙만으로는 부족했다. 등장 애니메이션
`logoFocus` 가 **매 키프레임에서 `filter` 를 통째로 덮어써서** 로고가
나타나는 1초 동안 여전히 안 보였다. 밝은 배경용 쌍둥이 키프레임
`logoFocusLight` 를 따로 뒀다. `invert` 를 **앞에** 두는 게 요점이다 —
먼저 잉크를 뒤집고 그다음 `brightness(2.4)` 가 밀어 올리므로, 어두운
배경에서 하얗게 번지던 연출이 밝은 배경에서도 배경 쪽으로 번진다.

README 는 `<picture>` + `prefers-color-scheme` 으로 GitHub 의 라이트/
다크 테마에 각각 대응한다. 밝은 배경용 마크
`webapp/static/plutus_mark_light.png` 를 새로 만들었다 —
`tools/make_light_mark.ps1` 가 알파는 그대로 두고 잉크만 바꿔 굽는다
(PIL 이 없으므로 System.Drawing 을 쓴다).

측정값 — 흰 배경 위 보이는 픽셀

    고치기 전   0 개
    고친 후     8,578 개  (평균 대비 0.798)
    어두운 배경 8,578 개  (평균 대비 0.803)  ← 변화 없음

### 정리 — 커밋 히스토리에서 공동작업자 트레일러 제거
커밋 44개에 붙어 있던 `Co-Authored-By` 트레일러를 전부 지웠다.
GitHub Contributors 에서 사라졌다. **파일 내용은 한 바이트도 바뀌지
않았고**(트리 해시 동일) 릴리스 36개와 첨부 자산도 그대로다 —
태그를 지우지 않고 `--force` 로 옮겼기 때문이다.

`tools/strip_coauthor.py` 로 재현할 수 있다.

---

## [3.4.3] — 2026-08-20

### 추가 — 설치 프로그램 `Plutus-Setup-x64.exe` ★
전에는 새 PC 마다 **사람이 하나씩** 했다. zip 풀고, WebView2 확인하고,
바로가기 만들고, 시작 메뉴에 등록하고. 낯선 PC(PC방 등)일수록 빠뜨리는
게 많았고, 빠뜨리면 앞선 버전들이 겪은 증상이 그대로 재현됐다.
그 전부를 설치 프로그램이 한 번에 한다.

받아서 실행하면 끝이다.

- **WebView2 런타임을 알아서 깐다.** 레지스트리 세 곳(64/32비트 시스템,
  사용자)을 다 보고, 없을 때만 마이크로소프트 공식 부트스트래퍼를 무인
  설치한다. 설치 후 다시 확인해서 여전히 없으면(네트워크 차단 등)
  조용히 넘기지 않고 알려 준다 — 이 경우 앱은 브라우저 모드로 돈다
- **시작 메뉴 등록** — 윈도우 검색에 "Plutus" 가 뜬다. 바탕화면
  바로가기만으로는 색인되지 않는다
- 바탕화면 바로가기는 체크박스로 선택
- **프로그램 추가/제거**에 정식 등록. 제거 프로그램 포함
- 업데이트 설치 시 실행 중인 앱을 알아서 닫는다

**관리자 권한(UAC)이 필요 없다.** `%LOCALAPPDATA%\Programs\Plutus` 에
사용자 권한으로 깐다. Program Files 를 쓰지 않는 건 취향이 아니라 필수다
— Plutus 는 실행 파일 옆 `.data\` 에 키·계정 DB·캐시를 쓰는데 Program
Files 는 관리자만 쓸 수 있어서, 거기 깔면 앱이 자기 데이터를 못 쓴다.
PC방·회사 PC 처럼 관리자 권한이 없는 곳에서도 그냥 깔린다.

제거할 때는 **설정을 남길지 물어본다.** `.data\` 에는 API 키와 계정
정보, 저장된 보고서가 들어 있다. 말없이 지우지 않는다. 무인 제거
(`/VERYSILENT /SUPPRESSMSGBOXES`)에서는 안전한 쪽 — 남기는 쪽으로
답한다.

설치본은 60.7MB 로 zip(83.9MB)보다 작다. WebView2 부트스트래퍼까지
넣고도 그렇다 (lzma2/max).

zip 배포도 그대로 유지한다. 압축만 풀어 쓰는 portable 방식을 좋아하는
경우와, 설치 프로그램이 막히는 환경을 위해서다.

곁가지
- 설치 프로그램이 바로가기를 만들었으면 앱이 첫 실행 때 또 묻지 않는다
  (`.data\shortcuts_asked` 를 미리 남긴다)
- WebView2 부트스트래퍼는 **저장소에 커밋하지 않는다.** "외부 저작물
  0개" 를 유지한다. 대신 빌드할 때 공식 permalink 에서 받고
  **Authenticode 서명을 확인**한다 — Valid + Microsoft Corporation 이
  아니면 빌드를 중단한다
- `tools/release.py` 가 설치본까지 굽고 릴리스에 자산 두 개를 올린다.
  Inno Setup 이 없으면 건너뛴다 — 설치본이 없다고 릴리스를 막을 이유는
  없다

> 코드 서명은 아직 안 했다. 처음 실행할 때 SmartScreen 경고가 뜬다.
> `추가 정보` → `실행` 으로 넘어간다.

---

## [3.4.2] — 2026-08-20

### 고침 — WebView2 런타임이 없으면 아무 말 없이 죽던 문제 ★
**다른 PC 에서 "개 버그남" 의 정체.** Edge WebView2 런타임이 없었다.

pywebview 는 윈도우에서 이 런타임 위에 화면을 그린다. 없으면 창을
띄우다 실패하는데, EXE 는 `console=False` 라 **그 오류가 아무 데도
보이지 않는다.** 사용자 눈에는 그냥 "안 켜짐" 이다. 로그에도 안 남는다.
가장 나쁜 종류의 실패다 — 증상이 없으니 원인을 찾을 단서도 없다.

윈도우 11 과 최근 윈도우 10 에는 기본 탑재라 개발 PC 에서는 영원히
재현되지 않는다. 정리된 이미지 · LTSC · Edge 를 제거한 PC 에는 없다.

이제 창을 만들기 **전에** 레지스트리에서 런타임을 확인하고, 없으면

1. 네이티브 대화상자로 이유를 설명한다 (콘솔이 없어도 보인다)
2. 원하면 마이크로소프트 공식 설치 페이지를 열어 준다
3. **그리고 기본 브라우저로 앱을 띄운다** — Plutus 는 로컬 웹앱이라
   브라우저에서 기능이 전부 그대로 돈다. 설치를 강요할 이유가 없다

런타임 점검을 통과하고도 창 생성이 실패하는 경우(그래픽 드라이버,
손상된 런타임, 정책으로 막힌 사용자 데이터 폴더)도 같은 식으로 잡는다.
예외를 삼키지 않고 대화상자에 타입과 메시지를 띄운 뒤 브라우저로 넘긴다.
**조용히 죽는 경로를 없앴다.**

곁가지로 잡은 것: 이 경로의 `print` 는 ASCII 만 쓴다. em-dash 하나가
cp949 콘솔에서 `UnicodeEncodeError` 를 내는데, 하필 "이미 뭔가 잘못된"
상황에서 로그 리다이렉트마저 실패했다면 그 print 가 앱을 죽인다.

### 변경 — 저장소 이름 `i_always_win` → `plutus`
제품명이 Plutus 인데 저장소만 옛 이름이었다. GitHub 에서 먼저 리네임한
뒤 코드의 URL 을 따라 고쳤다. 옛 주소는 GitHub 가 웹·API 모두
리다이렉트하므로 **구버전 앱의 업데이트 확인도 계속 동작한다.**

    https://github.com/tenkojun/plutus

같이 안 바꾼 것 둘 — 둘 다 **배포된 자원의 식별자**라 이름만 바꾸면
깨진다.

- `iaw-auth.tenkojun.workers.dev` — 중앙 인증 Worker. 바꾸면 로그인이 끊긴다
- `%LOCALAPPDATA%\i_always_win` — 앱 폴더가 쓰기 불가일 때의 폴백 데이터
  폴더. 바꾸면 기존 PC 의 키와 계정 DB 를 앱이 못 찾는다. 옮기려면
  마이그레이션 코드가 먼저 있어야 한다

---

## [3.4.1] — 2026-08-20

### 추가 — 진단 도구 `진단.ps1` 동봉
다른 PC 에서 안 뜬다는 보고가 들어왔는데 **원인을 물어볼 수단이 없었다.**
배포본을 받은 PC 에는 Python 이 없다. 진단 도구가 Python 을 요구하면
정작 필요한 곳에서 못 쓴다. 그래서 PowerShell 로 썼다 — 윈도우엔 항상 있다.

zip 을 풀면 `Plutus.exe` 옆에 `진단.ps1` 이 같이 나온다.

```
powershell -ExecutionPolicy Bypass -File 진단.ps1
```

보는 것
- **WebView2 런타임 유무** — pywebview 가 그 위에 화면을 그린다.
  없으면 창이 비거나 열리자마자 닫힌다. 새 PC 에서 안 뜨는 1순위 원인
- `_internal` 파일 수 · `python312.dll` 유무 — 압축이 덜 풀렸는지
- `Zone.Identifier` — 인터넷에서 받은 파일 차단 표식(SmartScreen)
- 설치 위치 — Program Files(쓰기 막힘) · Temp(zip 안에서 바로 실행) 경고
- `.data` 실제 쓰기 가능 여부 (권한을 물어보지 않고 써 본다)
- 포트 8765 를 **어느 프로세스가** 점유했는지
- 로그인 · 시세 · 업데이트 3개 호스트 443 연결 + 프록시 설정
- `app.log` 에서 오류 줄만, 윈도우 이벤트 로그의 Plutus 관련 항목
- 결과를 `plutus-진단.txt` 로도 저장

**개인정보는 담지 않는다.** API 키는 값이 아니라 존재 여부만 보고,
경로의 사용자 이름은 `<USER>` 로 가린다 — 결과를 그대로 복사해 붙여도
안전하게.

BOM(`utf-8-sig`)으로 저장한다. 없으면 PowerShell 5.1 이 cp949 로 읽어
한글이 깨지고 파싱 자체가 실패한다 (업데이터에서 이미 겪은 함정).

개발 환경용으로는 같은 점검을 하는 `tools/diagnose.py` 를 뒀다.

---

## [3.4.0] — 2026-08-20

### 추가 — 바탕화면 바로가기 · 윈도우 검색 등록
Plutus 는 압축만 풀면 도는 portable 배포라 **설치 프로그램이 없다.**
그래서 설치 프로그램이 해 줄 일(바로가기 · 시작 메뉴 등록)을 아무도
하지 않았고, 윈도우 검색창에 "Plutus" 를 쳐도 나오지 않았다.

첫 실행 때 한 번 물어본다. 설정 → 시스템 → `바로가기 만들기` 로도 부른다.

**검색에 뜨려면 시작 메뉴 등록이 필요하다.** 바탕화면 바로가기만으로는
색인되지 않는다 — 검색은
`%APPDATA%\Microsoft\Windows\Start Menu\Programs` 를 본다.
그래서 시작 메뉴 쪽이 본체고 바탕화면은 선택으로 뒀다.

의존성은 늘리지 않았다. pywin32 대신 Windows 에 원래 있는
`WScript.Shell` COM 을 PowerShell 로 호출한다.

작업 중 잡은 것 둘
- 소스 실행 중 `sys.argv[0]` 이 `-`(stdin) 나 모듈 경로일 수 있다. 그
  값으로 만들면 **눌러도 아무 일이 안 일어나는 죽은 링크**가 생긴다.
  경로를 확신할 수 없으면 만들지 않고 사유를 돌려준다.
- 설명의 em-dash(—)가 COM 을 거치며 `?` 로 깨졌다. 한글은 멀쩡한데 이
  문자만 상한다 — 하이픈으로 바꿨다.

실측: 바탕화면 1,803B · 시작 메뉴 1,851B 생성, 대상·아이콘·설명 정상.

### 변경 — 부팅 로그를 와이드 모니터에 맞게
v3.3.1 에서 평균 115자로 늘렸는데 1883px 화면에서는 여전히 절반
(≈820px)이었다. 모노스페이스 11.5px 기준 7.15px/char 이므로 화면을
채우려면 **250자 안팎**이 필요하다.

전면 재작성해 **평균 218자 · 최대 245자**. 실측 렌더 폭 1613px 로
1280px 창에서 126% — 넘쳐서 잘린다. 잘려도 뜻이 통하도록 중요한 정보를
앞에, 파라미터·부연을 뒤에 배치했다.

### 변경 — 로고 등장 이펙트
단순 페이드였다. 네 겹을 겹쳤다.

    블록  아래에서 올라오며 스케일 인      bootRise  .72s
    로고  블러가 걷히며 초점이 맞는다      logoFocus .95s
    광륜  뒤에서 빛이 퍼졌다 잦아든다      logoHalo  1.5s
    스윕  광택이 한 번 훑고 지나간다       logoSweep 1.1s

전부 transform/opacity 기반이라 레이아웃 재계산이 없다. 광륜은
`color-mix` 로 테마색에서 파생하므로 테마를 따라간다.

### 고침 — 로그인 후 앱이 먼저 보였다가 환영창이 뜨던 문제
v3.2.0 에서 고쳤다고 했으나 **자동 로그인 경로만** 고쳤고, 직접
아이디를 입력하는 경로는 그대로였다. 그쪽은 로그인 마스크를 먼저 걷고
`postLoginInit()` 안에서 `await checkAuth()` 로 네트워크를 기다린 뒤
환영창을 띄웠다 — 그 대기 동안 차트가 노출됐다.

이제 **환영창을 먼저 올리고 마스크를 걷는다.** 이름은 로그인 응답에
이미 들어 있으므로 네트워크를 기다릴 이유가 없었다.

---

## [3.3.1] — 2026-08-20

### 변경 — 부팅 로그를 옆으로 길게
직전 버전에서 줄 **수**를 늘렸는데, 정작 필요했던 건 한 줄의 **길이**
였다. 기존 줄은 평균 39자라 화면 왼쪽 4분의 1만 채웠다.

진짜 `dmesg` 는 타임스탬프 · 주소 · 크기 · 플래그가 붙어 한 줄이 100자를
넘는다. 그 밀도가 있어야 "쫙 흐르는" 느낌이 난다. 전면 재작성했다.

    [   0.001884] [MEM] reserving simulation heap: 512.0 MiB @ 0x00007ff4c0000000  policy=first-touch  numa_node=0  thp=always
    [   0.088723] [GATE] G4 ml overfit/calib    PBO>=50% | DSR<90% | gap>15% | resolution<=1e-4      -> DISABLE_MODULE

**평균 39자 → 115자** (최대 126자). 실측 렌더 폭 822px / 터미널 폭
768px — 화면을 넘겨 잘린다. 줄 수는 126 → 106 으로 줄었지만 화면에
찍히는 글자는 훨씬 많다.

`text-overflow: ellipsis` 를 제거했다. `…` 이 붙으면 로그가 아니라
"생략된 표" 처럼 보인다 — 진짜 커널 로그는 그냥 잘려 나간다.

내용도 실제 시스템 값으로 채웠다: 메모리 맵 주소·페이지 수, CPU 캐시
크기, PBKDF2 파라미터, 프로바이더별 요청 한도, 게이트 임계와 조치,
모듈별 알고리즘 이름, 정책 7종.

작업 중 잡은 것: 정지 지점 표를 재생성할 때 **콜론이 빠져** `2 501,`
로 나왔다. 그대로 두면 JS 구문 오류로 앱 전체가 죽는다.

---

## [3.3.0] — 2026-08-20

### 고침 — 부팅 화면이 사용자 테마를 무시하던 문제
테마 적용(`_initAppearance`)이 스크립트 **아래쪽**에 있었는데 부팅
시퀀스는 그보다 훨씬 위에서 돈다. 그래서 무슨 테마를 골라 뒀든
**부팅 로그는 늘 기본 시안색**으로 흘렀다.

저장된 테마를 `<head>` 최상단 스크립트에서 즉시 적용하도록 옮겼다.
이후 모든 렌더가 그 팔레트를 쓴다.

부팅 화면의 하드코딩 색도 전부 변수로 바꿨다.

    배경   #000      → var(--bg)
    로그   #8ff5d8   → var(--txt)
    경고   #ffd166   → var(--amber)
    흐림   #4a5c66   → var(--txt-mute)
    플래시 rgba(61,240,255,.30) → var(--hover)

실측(sakura 적용 시): 배경 `rgb(26,14,26)` · 로그 `rgb(255,159,200)` ·
흐림 `rgb(128,80,106)`. 9개 테마 전부 따라간다.

작업 중 잡은 것: 초기 스크립트가 `iaw.appPref` / `iaw.customTheme` 를
읽고 있었는데 실제 키는 `iaw.appearance.v1` / `iaw.theme.custom` 이다.
틀린 키는 **예외도 안 나고 조용히 아무 일도 안 한다** — 화면상으로는
"테마가 안 먹네" 로만 보인다.

### 변경 — 부팅 로그 85 → 126줄
메모리·CPU 초기화, 보안 정책 5종, 데이터 무결성 검사, 게이트 매트릭스
G1~G8, 정책 선언 4종, 터널·인증 상태를 추가했다. 기본 간격을 26ms →
22ms 로 줄여 길어진 만큼 늘어지지 않게 했다.

정지 지점도 새 구획 경계에 맞춰 7곳으로 재배치했다.

### 문서 — CLAUDE.md 최신화
v2.14.0 시점에서 멈춰 있었다. 이미 삭제된 `main.py` ·
`engine/institutional/` · `engine/risk/` 등이 구조도에 남아 있어, 새
세션이 이 파일을 읽으면 없는 경로를 참조하게 된다.

갱신: 버전 3.2.1 · jiqtx 33모듈/15,601줄 · 라우트 125 · 용어 66 ·
MIT 라이선스 · 등급 3단계 · 릴리스 도구 · 업데이터 · 간단 리서치.
빠지기 쉬운 함정(BOM · `--latest=false` · 테마 조기적용 · `--up` 은
한국식 빨강 · 툴팁 fixed 레이어)을 규칙으로 적었다.

---

## [3.2.1] — 2026-08-20

### 추가 — 로그인창 아래 버전 표시
마인크래프트처럼 로그인 화면 하단에 `Plutus v3.2.1` 과 개발자를
띄운다. 지금 무엇이 돌고 있는지 보이면 "업데이트가 됐나?" 를 확인하려고
설정을 열 필요가 없다.

인증이 필요 없는 `/api/app/info` 를 쓴다 — **로그인 전에도 보여야**
하기 때문이다. 조회에 실패해도 조용히 넘어간다(버전 표시 때문에
로그인이 막히면 안 된다).

`.login-mask` 를 `flex-direction: column` 으로 바꿨다. 기본 row 에서는
버전이 로그인창 **옆**에 붙는다.

---

## [3.2.0] — 2026-08-20

### 추가 — 간단 리서치
분석 결과를 처음 보는 사람도 읽을 수 있는 형태로 함께 만든다.
**같은 분석**을 쓰고 보여 주는 방식만 바꾸므로 계산 비용이 없다.

절을 지표 이름이 아니라 **질문**으로 연다.

    이건 어떤 종목인가요?
    얼마나 위험한가요?
    지금 흐름은 어떤가요?
    무엇이 이 종목을 움직이나요?
    얼마나 사야 하나요?
    무엇을 보면 판단이 틀렸다는 걸 아나요?
    이 분석이 모르는 것

모든 숫자에 **체감 환산**을 붙인다. 변동성 30.8% 같은 값은 그 자체로는
아무 느낌이 없다.

> **±2.0%** 보통 하루 움직임
> 100만원이면 하루에 약 2.0만원 정도 흔들립니다.
>
> **-39%** 과거 최대 하락
> 100만원이면 약 38.5만원 손실.
>
> **354일** 가장 오래 물린 기간
> 약 1.4년 동안 원금을 회복하지 못한 구간이 있었습니다.

**쉽게 만든다고 정직함을 버리지 않는다.** 초보자용이라고 불확실한 걸
확실하게 말하면 그게 제일 위험하다. 엔진이 기권했으면 "방향 예측을
하지 않았습니다 — 낮은 점수를 준 게 아니라 아무 값도 내지 않은 것"
이라고 쉬운 말로 쓴다. `|t| < 2` 인 거시 변수는 아예 빼고 "연결이
뚜렷하지 않아 뺐다" 고 밝힌다.

마지막 절이 `이 분석이 모르는 것`이다. 생존편향·PIT 아님·기권 모듈을
모두 적는다 — 겸손이 아니라 사용 설명서다.

분석이 끝나면 **간단 리서치 / 전문 보고서** 두 버튼이 나온다.
간단 리서치 끝에서 전문 보고서로 넘어갈 수 있다.

실측: 59KB · 인라인 SVG 3종 · 외부 리소스 0 · 절 7개.

작업 중 잡은 것: 반증 조건 절이 통째로 사라져 있었다. `a.kill` 은
`KillCriterion` 리스트인데 `.criteria` 를 찾고 있어 늘 빈 값이었다 —
데이터는 있는데 화면에 없었다. 이제 기준·현재값·조치까지 보이고
이미 발생한 조건은 색으로 구분한다.

### 고침 — 로그인 직후 차트가 깜빡이던 문제
환영 화면을 `setTimeout(…, 400)` 으로 띄우고 있었다. 그 400ms 동안
앱 화면이 먼저 보였다가 덮였다. 인증 shield 를 걷기 **전에** 환영
화면을 올리도록 순서를 바꿨다.

**Enter**(또는 Space·ESC)로 넘어간다. 마우스를 쓰지 않아도 되게 했다.

---

## [3.1.1] — 2026-08-20

전체 점검에서 나온 결함을 고쳤다. 셋 다 **조용히 잘못 동작하던** 것들이라
화면상으로는 멀쩡해 보였다.

### 고침 — 무료 3회 한도가 완전히 우회되던 문제
`user_id` 가 `None` 인 상태로 `consume()` 이 불리면 그대로 진행해
`usage_daily` 에 NULL 키로 행을 넣었다. **SQLite 는 PRIMARY KEY 에
NULL 중복을 허용**하므로 매 호출이 새 행이 됐고, 카운트가 전혀 누적되지
않았다.

실측 — 10회 호출에 10회 전부 통과, DB 에 1행(n=10) 대신 10행 생성.

이제 `user_id` 가 없으면 진행하지 않고 재로그인을 안내한다.

### 고침 — DB 잠금 시 한도가 열리던 문제
`consume()` 이 예외를 잡아 `ok=True` 로 통과시키고 있었다("계량 실패가
분석을 막을 이유는 없다"). 그런데 **잠금은 동시 요청이 많을 때 나는
것**이라, 정확히 한도를 지켜야 하는 순간에 문이 열린다.

실측 — 이미 99회 쓴 무료 계정이 잠금 상황에서 통과.

이제 이미 한도를 넘긴 게 확인되면 막고, 판단 자체가 불가능할 때만
통과시킨다. 덧붙여 연결을 WAL 모드 + busy_timeout 20초로 바꿔
잠금 충돌 자체를 줄였다.

### 고침 — 아무 사용자나 서버를 재시작시킬 수 있던 문제
`/api/update/start` 와 `/api/update/apply` 에 `@require_auth` 만 걸려
있었다. 이 앱은 터널로 **여러 명이 한 인스턴스에 붙는** 구조라, 로그인만
하면 누구나 서버를 통째로 내렸다 올릴 수 있었다.

확인(`check`)은 그대로 두고 받기·교체만 관리자 전용으로 바꿨다.

### 고침 — 수급 캐시가 lookback 을 무시하던 문제
캐시 키가 `market:top:유니버스크기` 라 `lookback` 을 바꿔도 예전 결과가
나왔다. 20일 기준과 60일 기준이 같은 칸을 쓰고 있었다.

### 고침 — 호버 추적이 제거된 DOM 을 붙잡던 문제
위젯은 자주 통째로 다시 그려지는데 `_lastHoverEl` 이 사라진 노드를 계속
참조해 서브트리가 GC 되지 않았다. `isConnected` 를 함께 본다.

### 확인 — 문제가 아니었던 것
업데이터의 `zipfile.extractall` 에 경로 검사가 없어 zip slip 을 의심했다.
악성 zip(`../../ESCAPED.txt`)을 실제로 넣어 보니 Python 3.12 의 zipfile 이
자체 정화해 스테이징 **안**으로 들어왔다. 탈출 없음 — 고칠 것 없음.

### 점검 범위
engine 전 모듈 임포트(실패 0) · GET 라우트 전수 · 프론트 정합성
(정의 안 된 함수 0 · 없는 DOM id 0) · 타이머/리스너 누수 ·
다자산 회귀(AAPL·GLD·TLT·BTC·^KS11 전부 OK, 외부 리소스 0)

---

## [3.1.0] — 2026-08-20

### 추가 — GitHub 자동 업데이트

새 버전이 올라오면 앱이 확인하고 물어본다.
  로그인 6초 뒤 조용히 확인 → 모달 → 받기·검증·해제 → 재시작 → 교체

GET /api/update/check · POST /api/update/start
GET /api/update/status · POST /api/update/apply

설정은 건드리지 않는다
교체 대상은 Plutus.exe 와 _internal/ 뿐. API 키·계정 DB·위젯 배치·
보고서 보관함이 든 .data/ 는 손대지 않는다.

실행 중인 exe 는 윈도우가 잠그므로 앱 안에서 못 바꾼다. detached
PowerShell 이 앱 종료를 기다렸다가 교체하고, 기존 파일은 .old 로 밀어
둔다. 새 버전이 6초 안에 기동하지 못하면 자동으로 되돌린다.

잡은 위험: 릴리스 zip 에 .data 가 섞이면 사용자 키가 덮인다
Copy-Item $new\* 는 PowerShell 에서 점으로 시작하는 폴더도 포함한다.
두 겹으로 막았다 — (1) 압축 해제 단계에서 .data 를 지우고
(2) 교체 스크립트가 최상위를 돌며 .data 를 건너뛴다.
실측: .data/keys.json 을 넣은 악성 zip 이 스테이징에서 제거됨.

검증 실패는 정확한 원인을 보고한다. 예전 임의 1024B 임계값이 먼저
걸려 "exe 없는 zip" 까지 "비어 있습니다" 로 보고했다 — 진단이 틀리면
엉뚱한 곳을 고친다. 없는파일/0바이트/zip아님/exe없음 각각 다른 메시지.

버전 비교는 v 접두사와 -beta 꼬리표를 무시하고 숫자 세 자리만 본다.
7개 경계 케이스 통과.

소스 실행 중에는 자동 교체 안 함 — 로컬 수정본을 덮어쓸 수 있어
git pull 을 안내한다.

docs/RELEASE.md — 릴리스 발행 절차. 압축 전 .data 삭제 경고 포함.

---

## [3.0.1] — 2026-08-19

### 추가 — Plutus 로고 적용

부팅 화면에 로고를 띄운다. 이미지에 워드마크가 이미 있어 아래 텍스트
두 줄은 내렸다 — 같은 글자가 두 번 나오고 있었다. 65px → 225px.

tools/make_icons.py — 원본 하나에서
  plutus_mark.png  검은 배경 → 투명 (부팅용)
  favicon.png      엠블럼만 크롭한 256px
  app.ico          16~256 멀티사이즈
아이콘은 16px 까지 줄어들어 워드마크가 얼룩이 되므로 엠블럼만 쓴다.
크롭 좌표는 밝기 프로파일에서 빈 띠를 찾아 매번 계산 — 로고를 바꿔도
따라간다.

사고 기록: 로고 원본을 파괴했다
투명 변환에서 알파를 int16 으로 계산해 (lum-14)*255 가 오버플로,
가장 밝은 선이 음수로 감싸며 0(완전 투명)이 됐다. 그걸 원본에
덮어썼고 파생본도 손상본에서 재생성돼 복구 경로가 없었다.

두 가지를 동시에 잘못했다 — 계산 오류 + 원본 덮어쓰기. 후자가
없었으면 복구할 수 있었다. 도구를 고쳤다:
- 산출물은 다른 파일명으로만. 원본은 읽기 전용 취급
- 알파 계산 float32
- 저장 전 불투명 픽셀 검사, 0.5% 미만이면 중단

재생성 실측: 불투명 18.4%(사고 당시 2.1%), 엠블럼 594x594.
부팅 이미지는 mark → 원본 → fox 3단 폴백이라 자산이 없어도 안 깨진다.

---

## [3.0.0] — 2026-08-19

### 변경 — Plutus 리브랜딩 · 플래티넘 등급 · UI 사운드

제품명 I ALWAYS WIN → Plutus (그리스 신화 부의 신). 42파일 87곳 치환.
부제 RESEARCH & ANALYTICS.

인프라 식별자는 그대로 뒀다 — iaw-auth Worker URL 과 저장소 주소는
이미 배포돼 돌아가는 자원이라, 같이 바꾸면 로그인이 끊기고 원격
저장소가 사라진다. 옮기려면 Cloudflare/GitHub 에서 먼저 리네임할 것.

로고는 webapp/static/plutus.png. 파일이 없으면 기존 이미지로 폴백한다.
tools/make_icons.py 로 로고 하나에서 파비콘 + 멀티사이즈 ico 생성.

플래티넘 등급
무료/프리미엄 2단계 → 3단계. 한도 판정을 tier=="premium" 하드코딩에서
기능 표 조회로 바꿔 등급 추가 시 조건문을 안 고쳐도 되게 했다. 모르는
기능 이름은 막는다(닫힌 기본값).
실측 무료 OOOXX · 프리미엄 OOOOO · 플래티넘 OOOOO, 터널은 플래티넘만.

UI 사운드
호버/클릭 사운드. 호버는 클릭의 1/4 음량(0.0106 vs 0.0428)이고 연타를
억제한다 — 30회 연속 호출에 1회만 발음. 개별 요소에 달면 동적 DOM 이
빠지므로 document 위임. soundUi 로 따로 끌 수 있다.

부팅 테마음
A단조 → C장조로 열리는 진행. 저역 → 5도 → 옥타브 → 장3도 순으로
쌓아 마지막에 밝게 푼다. 이 해결이 없으면 그냥 웅웅거림으로 들린다.

한경 프리미엄 확인 결과
무료 RSS(/feed/finance)는 이미 연동돼 있다. 프리미엄 유료 콘텐츠는
공개 API 가 없고 RSS 에도 안 나오며 스크래핑은 약관 위반(RSS 도
상업적 이용·AI 학습 금지 명시). 애널리스트 전용 피드도 없다.
대안은 KIS OpenAPI(이미 클라이언트 있음)와 Finnhub/FMP.

---

## [2.22.0] — 2026-08-19

### 추가 — 용어집 44 → 66개

보고서가 쓰는 용어를 감사했더니 29개가 설명 없이 등장하고 있었다.
용어를 쓰면서 설명하지 않으면 읽는 사람이 판단을 할 수 없다.
실질적 가치가 있는 22개를 추가했다.

  성과   샤프 소르티노 칼마 왜도 첨도 표준오차 신뢰구간
  분포   분위수 팻테일 점프 베타 헤지
  미시   스프레드 유동성 임팩트
  채권   듀레이션 모멘텀 밸류
  검증   자기상관 과적합 교차검증 리밸런싱

기존 원칙(정의만 쓰지 않고 왜 보는지 + 어떤 값이면 문제인지)을 유지.

실측: 66개 중 60개가 본문 등장, 총 440회 매칭. 부분문자열 충돌 0 —
'베타' ⊂ '하방 베타' 같은 관계에서 긴 쪽이 우선 매칭됨을 확인.
미등장 6개는 조건부 섹션 전용(ML 게이트 통과 시에만 나오는 PSR 등).

작업 중 잡은 것
- 생성 스크립트가 인접 문자열 리터럴을 이어 붙이며 줄 경계 공백을
  먹어 "쉽게부풀려지므로" 처럼 붙어 나왔다
- 마크다운 **볼드** 를 넣었는데 툴팁은 textContent 라 별표가 글자로
  보였다

다자산 검증: AAPL·GLD·TLT·BTC·SOXL·KO·^KS11 7종 전부 성공.
섹션 25~30개로 자산별 분기(applies 정상), 외부 리소스 전부 0.

---

## [2.21.0] — 2026-08-19

### 고침 — 전수 점검에서 나온 결함 4건

53개 GET 라우트를 전부 때려 보고, 프론트 정합성·CSS 변수·엔진 검증
스위트를 돌려 나온 것들.

1) /api/heatmap/treemap 타임아웃 (43초)
23종목의 시세+시총을 직렬로 왕복하고 있었다. 1종목당 실측 1.9초
(quote 1.2s + marketcap 0.6s) → 23종목이면 43초라 프론트가 끊긴다.
대기가 대부분 네트워크라 ThreadPoolExecutor(12) 로 겹쳤다.
43.3초 → 4.6초.

2) /api/llm/status 500
engine/llm/hardware.py 가 psutil 을 무조건 import 했다. psutil 은 이
프로젝트의 필수 의존이 아니라 미설치 환경에서 라우트가 통째로 죽었다.
하드웨어 정보 하나 때문에 화면이 죽을 이유가 없다 — Win32
GlobalMemoryStatusEx / POSIX sysconf 폴백을 넣고 추정 여부를 source
필드로 알린다. 500 → 200.

3) .mf-note 가 --warn 을 참조 — 앱 팔레트에 없는 이름
--warn 은 **보고서** 팔레트 변수다. 앱에는 --amber 다. 늘 폴백 색으로
떨어져 테마를 바꿔도 이 줄만 안 따라왔다.

4) 오해 소지 있는 CSS 폴백 6곳 제거
var(--up,#2ec27e) 처럼 앱에 이미 있는 변수에 **서구 관례 색**을 폴백으로
달아 뒀다. 앱은 한국 관례(상승=적 #ff5a52)라 변수가 빠지는 순간 의미가
뒤집힌다.

UI — 시장 개요가 잘려 보이던 문제
타일이 화면보다 540px 넓은데(잘린 타일 4개) 스크롤바를 숨겨 둬서
"더 있다" 는 신호가 전혀 없었다. 양 끝 페이드(스크롤 위치에 따라
has-l/has-r)와 얇은 스크롤바를 넣고, 세로 휠을 가로 스크롤로 돌렸다.

검증
- 엔진 검증 스위트 7항목 전부 통과 (아키타입 5/5, PEAD 검출률 100%,
  ACI 커버리지 오차 0.5%p 이내, Murphy 판별비 411배)
- 다자산 실행: TLT R²=85.3%(국채 밴드 적중), BTC NO_TRADE(거부권 1건,
  MDD -76.6%), SOXL 레버리지 ETP 분류 + 스프레드 287bp 거래불가
- 서버 예외 0건, 5xx 0건

---

## [2.20.0] — 2026-08-19

### 빌드 — matplotlib · PIL 제외 (−28MB)
v2.15.0 에서 구엔진 `engine/report/plotter.py` 를 지우면서 그리기
라이브러리가 필요 없어졌는데도 계속 딸려 들어와 26MB 를 먹고 있었다
(matplotlib 15M + PIL 11M). 보고서 차트는 인라인 SVG 를 **문자열로**
만들기 때문에 그리기 라이브러리가 아예 없어도 된다.

import 를 차단한 상태에서 `engine` · `engine.jiqtx` · `charts` ·
`dynamic_report` · `market_flow` · `quota` · `webapp.server` 전부
임포트되는 것을 확인한 뒤 excludes 에 넣었다.

빌드 221MB → **193MB**, exe 20.5MB.

### 개선 — 효과음이 밋밋했던 이유
v2.19.0 의 합성음은 **오실레이터 하나 + 단순 포락선**이었다. 소리는
나지만 얇고 말라서 "삐" 에 가까웠다. 세 가지를 더했다.

**1. 마스터 버스 + 합성 리버브**
오실레이터를 `destination` 에 바로 꽂으면 공간이 없다. 지수 감쇠
노이즈 IR(1.1초)에 이른 반사 4개를 심어 26% 만 적신다. 이것 하나가
'기계실에서 나는 소리' 를 만든다. 고역은 highshelf 로 −4dB 깎아
디지털 쨍함을 줄였다.

**2. FM 합성**
단일 사인파는 포락선을 아무리 다듬어도 배음이 없다. 변조기를 캐리어
주파수에 꽂고 변조 깊이를 감쇠시키면 음색이 닫히면서 금속·벨 느낌이
난다. 실측 — 같은 기음의 순수 사인 대비 zero-crossing **2.2배**
(108 → 238).

**3. 디튠 2겹 + 스테레오 배치**
한 겹은 얇다. 살짝 어긋난 두 겹을 좌우로 벌려 두께를 만든다.

실측 리버브 기여: `granted` 의 0.7~1.1초 잔향 `0` → `0.00053`.

소리의 좋고 나쁨은 결국 귀로 판단할 문제다. 위 수치는 **의도한
메커니즘이 실제로 동작하는지**만 확인한 것이다.

---

## [2.19.0] — 2026-08-19

### 라이선스 — MIT 로 되돌림
eDEX-UI 자산을 전부 걷어내고 **직접 만든 것으로 대체**했다.
`webapp/static/audio/` 삭제 · `NOTICE.md` 삭제 ·
`tools/fetch_edex_assets.py` 삭제 · `LICENSE` → MIT.

이제 외부 저작물이 하나도 없다. 소스 공개 의무 없이 EXE 단독 배포와
상용 이용이 가능하다.

### 변경 — 효과음 13종을 Web Audio 실시간 합성으로
**오디오 파일이 0개다.** 터미널 효과음은 짧은 파형 몇 개의 조합이라
합성으로 충분히 재현된다. 덕분에

- 라이선스 문제가 원천적으로 없고
- 배포 용량이 늘지 않으며 (원본 자산은 2.3MB 였다)
- 나중에 테마별로 음색을 바꾸는 것도 공짜다

원본 효과음의 **성격**만 참고했다 — 짧은 디지털 틱(stdout), 상승 2음은
승인(granted), 하강 + 거친 배음은 거부(denied), 밴드패스 스윕은
스캔(scan). 파형·주파수·포락선은 전부 새로 잡았다.

`_tone()`(스윕 + ADSR)과 `_hiss()`(노이즈 밴드패스 스윕) 두 원시 함수로
13종을 조합한다. AudioContext 는 하나만 만들어 재사용하고, 자동재생
정책 때문에 매번 `resume()` 을 시도한다.

검증: OfflineAudioContext 로 13종을 렌더해 피크 진폭 측정 —
전부 소리가 남 (0.022 ~ 0.158).

### 변경 — 부팅 로그 85줄, 정지 지점을 우리 구획에 맞춤
로그를 70 → **85줄**로 늘리고, 지연 정지를 **우리 로그의 구획 경계**에
걸었다.

    2번째  501ms   헤더 직후
    14번째 401ms   메모리·CPU 블록 끝
    38번째 301ms   데이터 소스 프로빙 끝
    58번째 301ms   엔진 모듈 적재 끝
    73번째 301ms   보고서 엔진 블록 끝
    마지막 둘 301ms · 그 외 26ms

실측 로그 구간 4,383ms · 전체 6.9초.

처음엔 구획 경계 8곳에 전부 정지를 걸었더니 5.2초로 늘어졌다.
흐름이 크게 바뀌는 다섯 곳만 남겼다.

---

## [2.18.0] — 2026-08-19

### 변경 — 부팅 화면을 eDEX-UI 원본으로
사운드 13종 · 부팅 로그 원문 · 줄별 지연 타이밍을 **그대로** 가져왔다.
제품명(`eDEX-UI` → `Plutus`, `eDEX` → `IAW`)과 로고(여우)만 우리 것.

타이밍은 eDEX 의 `_renderer.js` `displayLine()` 지연표를 그대로 이식했다.
균일 간격으로 흘리면 밋밋한데, 특정 지점에서 길게 멈추는 게 이 연출의
리듬을 만든다.

    2번째 줄   500ms
    4~24번째    30ms
    25번째     400ms
    42번째     300ms
    43~81번째   25ms
    마지막 둘  300ms
    그 외      pow(1-i/1000, 3) × 25ms

실측 86줄 → 로그 구간 3,835ms · 이후 300ms 대기 → 300ms 스냅 →
로고 2,600ms. 전체 7.0초. theme 사운드는 eDEX 와 같이 타이틀 화면에서
400ms 뒤 재생한다.

`stdout.wav`(티리릭)를 3줄에 한 번 울린다. 예전 합성 부팅음
(`_playBootSound`, F#장조 sustain)은 '우우웅' 하고 길게 깔려 blip 을
덮어서 제거했다.

**자산이 없어도 앱은 돈다.** `_sfxProbe()` 가 실패하면 Web Audio 합성음과
자체 부팅 로그 70줄로 조용히 폴백한다.

### 라이선스 — GPL-3.0 으로 변경
eDEX-UI 는 GPL-3.0 이고 GPL 은 전염성이라, 그 자산을 포함해 배포하는
순간 **결합물 전체가 GPL-3.0** 이 된다. 배포 시 전체 소스를 제공해야
하고 독점 배포는 불가하다. `LICENSE`(전문)와 `NOTICE.md`(출처·의미·
되돌리는 법)를 추가했다.

되돌리려면 `webapp/static/audio/` 를 지우면 된다 — 폴백이 살아 있어
기능 손실 없이 GPL 자산이 0 이 된다.

### 고침 — 부팅 화면이 영구히 갇히던 버그
사운드 뱅크(`let _sfxReady`)를 부팅 IIFE **아래**에 선언해 놓고
`async` 부팅 함수에서 참조했다. TDZ ReferenceError 가 조용한 rejection 이
되어 시퀀스가 그대로 멈췄고, `#boot` 오버레이가 화면에 영원히 남았다.

선언을 위로 옮기고, **12초 안전장치**와 `unhandledrejection` 핸들러를
달았다. 앞으로 연출 중 무엇이 터지든 화면은 반드시 걷힌다.

### 고침 — 부팅 로그의 HTML 엔티티 이중 이스케이프
eDEX 의 `boot_log.txt` 는 innerHTML 로 바로 넣는 용도라 `&lt;` `&quot;`
같은 엔티티가 **이미 들어 있다.** 여기에 우리 escape 를 한 번 더
적용해서 `&amp;lt;` 가 되고, 화면에 `&lt;` 가 글자로 찍혔다.
textarea 로 한 번 디코드한 뒤 렌더 시점에 한 번만 escape 한다.

### 추가 — eDEX 사운드를 앱 전반에
로그인 성공 `granted` · 실패 `denied`. 나머지(panels · expand · scan ·
alarm · error · info · folder · keyboard · stdin)도 뱅크에 올려 뒀다.
같은 소리가 겹쳐 나야 하므로 노드 4개를 풀로 돌려 쓴다.

### 도구
`tools/fetch_edex_assets.py` — 자산·부팅로그·GPL 전문을 받고 크기를
검증한다. 크기가 크게 어긋나면 리다이렉트/에러 페이지를 받은 것이라
경고한다.

---

## [2.17.0] — 2026-08-19

### 추가 — eDEX-UI 풍 부팅 시퀀스
터미널 로그가 좌르륵 흐르고 → **띠딕** 스냅 → 로고 → 로그인.

eDEX-UI 의 부팅 로그 **형식**을 따랐다 — 타임스탬프 없이 커널 스타일
메시지가 단계별로 몰렸다 흩어졌다 하며 70줄이 흐르고 `Boot Complete.`
로 끝난다. 한 프레임에 1~3줄씩 몰아 찍고 가끔 길게 멈춘다.

> **라이선스** — eDEX-UI 는 GPL-3.0 이다. 이 저장소는 독점이라 원문
> 로그와 사운드 파일을 가져오지 않았다. GPL 자산을 넣으면 배포되는
> 결합물 전체가 GPL 이 된다. 그래서 형식만 참고해 **우리 시스템 내용으로
> 새로 썼고**, 줄마다 나는 blip 도 Web Audio 로 합성한다.
> 원 저작물을 그대로 쓰려면 저장소를 GPL-3.0 으로 공개해야 한다.

언제든 SKIP / 클릭 / 아무 키로 건너뛴다. `prefers-reduced-motion` 이면
로고만 보여준다. **연출이 실제 준비를 늦추지 않는다** — `start()` 는
예정대로 부르고 화면만 위에서 덮는다.

### 고침 — 우측 상단이 비어 보이던 이유
`.widget-dock` 이 `top:175px ~ bottom:8px` 를 잡고 `justify-content:
flex-end` 라 아이콘이 화면 **바닥**에 몰렸다. chipbar 가 끝나는 지점부터
바닥까지가 통째로 빈 띠였다. 내용 높이만 차지하고 위쪽 정렬로 바꿨다.

작업 중 잡은 것: `top` 을 176px 로 박았더니 칩 4개(높이 190px)와
**64px 겹쳤다**. 상수 대신 chipbar 실측값으로 `--dock-top` 을 채운다.

### 변경 — 이모지 → 인라인 SVG
헤더와 사이드바의 💬 📋 ☁ 📜 🤖 를 선 아이콘으로 교체했다. 이모지는
OS 마다 다른 컬러 비트맵이라 어두운 터미널 배경에서 혼자 튀고,
`currentColor` 를 안 따라 hover 색이 먹지 않았다.

브랜드 부제도 고쳤다 — 이름을 두 번 쓰고 있었고 '백테스트' 는 v2.2 에서
제거된 기능인데 표기만 남아 있었다.

### 추가 — 시장 개요 클릭 → 속보 + 뉴스 캐러셀
타일을 누르면 속보와 일반 뉴스를 **함께** 연다. 위쪽은 4.2초마다 가로로
넘어가는 캐러셀(좌우 버튼 · 점 네비), 아래는 전체 목록.
한쪽 소스가 실패해도 다른 쪽은 보여준다.

### 추가 — 회원 등급 · 보고서 일일 한도
무료 3회 / 프리미엄 무제한. `GET /api/quota`,
`POST /api/quota/tier`(관리자 전용).

**한도는 서버에서 센다.** 프론트만 막으면 `/api/jiqtx/analyze` 를 직접
때리면 그만이다. 확인과 증가를 한 트랜잭션(`BEGIN IMMEDIATE`)에서 해서
동시에 두 번 눌러도 3회가 4회가 되지 않는다. 날짜 경계는 **서버 로컬**
기준 — 사용자 시간대로 자르면 시간대를 바꿔가며 초기화할 수 있다.

실측: 1·2·3회 허용 → 4·5회 429 차단 → 프리미엄 승격 후 무제한.

### 추가 — 보고서 차트 3종
| 차트 | 왜 |
|---|---|
| 지평 비교 (묶은 막대) | 표만으로는 **부호가 어디서 뒤집히는지** 안 보인다. 0선을 항상 그린다 |
| 낙폭 수중 곡선 | '최대낙폭 −34%' 로는 **얼마나 오래 물려 있었는지**를 알 수 없다 |
| 거시 기여도 토네이도 | 절대값 정렬 · 0 기준 좌우. `\|t\|<2` 변수는 **뺀다** |

작업 중 잡은 것: 낙폭 곡선이 안 나왔다. `a.prices` 를 DataFrame 으로
보고 `["close"]` 로 접근했는데 실제로는 **종가 1차원 배열**이었다.
`try/except` 가 그 실패를 조용히 삼켜 차트만 사라졌다. 접근을 고치고
삼키는 except 를 없앴다.

검증: SVG 27개 · 외부 리소스 0 · 4테마 미매핑 0.

### 고침 — 위젯 자동 갱신이 슬롯 A·B 만 보던 문제
T2/T3 템플릿은 슬롯이 A~D 인데 60초 갱신 루프가 A·B 만 확인해서
C·D 에 올린 위젯은 영영 갱신되지 않았다.

---

## [2.16.0] — 2026-08-19

### 추가 — 시장 수급 위젯
"오늘 어디로 수급이 몰리는가" 를 세 질문으로 나눠 답한다.

| 탭 | 답하는 질문 |
|---|---|
| 자금 이동 | 어제 **같은 시각** 대비 거래대금 비중이 어디로 옮겨갔나 (%p) |
| 매수 / 매도 | 지금 어디를 집중적으로 사고 파는가 (순매수 프록시 금액) |
| 섹터 강약 | 오늘 어느 섹터가 강세인가 |

어제와 비교할 때도 **같은 시각으로 자른다**. 오늘 오전 10시를 어제
하루 전체와 비교하면 언제나 "어제보다 줄었다" 가 나온다 — RVOL 에서
이미 겪은 함정이라 여기서도 같은 규칙을 적용했다.

섹터 강약은 **등락과 자금 방향이 함께 말할 때만** 라벨을 준다.
오르는데 순매도이거나 내리는데 순매수면 중립으로 남긴다.

순매수 프록시와 당일 등락이 어긋나면 행 아래 적는다 —
"하락 중 저가 매수세 (봉 내 매수, 일간은 하락)". CMF 는 봉 **안에서**
종가 위치만 보므로 하루 종일 흘러내리면서도 순매수로 잡힐 수 있다.
그대로 '집중 매수' 라고만 쓰면 오해를 부른다. 실측에서 NVDA 가
-2.3% 인데 순매수 1위(+$1.4B)로 나온 게 정확히 그 경우다.

`GET /api/market/flow` · 엔진 쪽 3분 캐시 (15분 지연 데이터라 충분).

### 고침 — 위젯 배치가 로그인할 때마다 초기화되던 문제
레이아웃을 `localStorage` 에만 저장하고 있었다. EXE 웹뷰는 프로필이
갈리면 저장소가 비고, 다른 브라우저나 터널로 들어오면 애초에 다른
저장소다. 그래서 매번 기본 배치로 돌아갔다.

**서버(사용자 prefs)를 정본으로 승격**했다. localStorage 는 첫 화면을
깜빡임 없이 그리기 위한 캐시로만 남기고, 그 위에 서버 값을 덮어쓴다.
저장은 드래그 연타를 묶어 600ms 후 한 번만 보낸다.

---

## [2.15.0] — 2026-08-19

### 고침 — 용어 툴팁이 잘리던 진짜 이유
"마우스 올리면 옆이 잘린다"는 제보를 따라가 보니, 화면 가장자리 문제가
아니었다. 보고서에는 **잘라내는 조상이 둘** 있다.

    details.sec { overflow:hidden }   ← 모든 섹션
    .tw         { overflow-x:auto }   ← 모든 표 감싸개

툴팁은 `.term` 안의 `position:absolute` 였다. absolute 는 저 조상 박스에서
잘린다. 그래서 **화면 한복판에 있어도** 섹션 가장자리나 표 안에서는 잘렸다.
붙는 방향을 바꾸던 기존 대응(tip-left/tip-right)으로는 고칠 수 없었다.
자르는 주체가 뷰포트가 아니었기 때문이다.

`<body>` 직속에 `position:fixed` 레이어 하나를 두고 내용만 갈아 끼우는
방식으로 바꿨다. fixed 는 overflow 조상을 벗어난다. 좌표는
`getBoundingClientRect()` 로 잡고 뷰포트 안으로 clamp 하며, 위가 좁으면
아래로 뒤집고, 화살표는 밀린 만큼 되돌려 계속 용어를 가리킨다.

실측(1280×720, 섹션+가로스크롤 표 안): 5개 위치 전부 뷰포트 이탈 0.

### 고침 — 용어의 절반이 조용히 툴팁을 못 얻고 있었다
같은 작업 중에 발견. 텍스트 노드 필터가 전역 정규식으로 `test()` 를
불렀다. `/g` 정규식의 `test()` 는 `lastIndex` 를 남기므로 다음 노드를
중간부터 검사한다. 결과가 `[true, false, true, false]` 로 **한 노드 걸러
하나씩 통째로 누락**됐다. 본문 순회 쪽은 `lastIndex = 0` 을 하고 있었는데
필터 쪽만 빠져 있었다. 눈에 띄지 않는 종류의 버그다 — 툴팁이 없어도
화면은 멀쩡해 보인다.

### 고침 — 툴팁이 테마를 타지 않았다
`themed_css(CSS, theme) + _tip_css()` 로 툴팁 CSS 를 테마 밖에 붙이고
있었다. 라이트/세피아에서 툴팁만 다크로 남고, 용어 밑줄·강조색이 밝은
바탕에서 대비를 잃었다. `themed_css(CSS + _tip_css(), theme)` 로 합치고
관련 6색을 팔레트에 편입했다.

밝은 바탕에서는 어두운 툴팁이 대비가 가장 좋아 그 방향으로 잡았다
(라이트 실측: 툴팁 13.40:1, 본문 17.46:1). 4테마 모두 미매핑 색 0.

### 제거 — 구엔진 47파일 6,359줄
`main.py` 의 13단계 오케스트레이터와, 그것만 참조하던 패키지 10종
(ml · risk · volatility · factor · institutional · signal_engine ·
explain · orderflow · analysis · report)을 지웠다. `engine/jiqtx` 가
전부 대체한다.

이들은 `arch` · `statsmodels` · `hmmlearn` · `torch` · `xgboost` 를
import 하고 있었는데 그 라이브러리들은 설치돼 있지도 않았다. try/except
폴백 덕에 죽지 않고 **성능만 낮아진 채** 돌고 있었을 뿐이다.

`POST /api/analyze` 와 잡 러너도 server.py 에서 제거했다(-121줄).
프론트는 이미 `/api/jiqtx/*` 만 쓰고 있었다. `engine/__init__.py` 의
지연 export 와 `app.spec` 의 hiddenimports·폰트 자산도 정리했다.

검증: `engine` · `engine.jiqtx` · `engine.data.loader` ·
`engine.analyze_history` · `webapp.server` 전부 임포트 정상.

---

## [2.14.0] — 2026-08-19

### 추가 — 앱 안에서 보고서 보기
보고서가 `target="_blank"` 로 열려 앱 밖 브라우저로 튕겨 나갔다.
EXE(pywebview)에서는 시스템 브라우저가 뜨면서 흐름이 끊긴다.
**앱 내부 대형 팝업**(iframe)으로 바꿨다.

- 상단 바: 글자 크기 A− / A+ · 보관함 · 다운로드 · 새 창(↗) · 닫기
- 같은 출처라 iframe 내부 문서의 글자 크기를 직접 조절한다
- 분석 결과·분석 이력(위젯/팝업)의 보고서 링크 3곳을 전부 뷰어로 연결

### 추가 — 보고서 보관함
`GET /api/reports` · `GET /api/reports/download/<file>` ·
`POST /api/reports/delete`

생성된 보고서를 최신순으로 나열한다(종목·종류·최신/보관·크기·시각).
열기 / 다운로드 / 삭제.

**경로 탈출 차단** — 사용자가 준 파일명을 그대로 join 하면 `../../` 로
저장소 바깥 파일을 내려받을 수 있다. `basename` + `realpath` +
공통 경로 검사로 막았다. 실측: `..%2f..%2fversion.py` → 404.

### 추가 — 보고서 테마 4종
`engine/jiqtx/report_theme.py` · 다크 / 라이트 / 세피아 / 고대비

보고서는 외부 리소스 0개의 자기완결 HTML 이라 **생성 시점에** 색이 굳는다.
그래서 CSS 의 하드코딩 색(고유 39색 · 108회 등장)을 기계적으로 CSS 변수로
치환하고, 테마별 `:root` 팔레트를 앞에 붙인다.
다크 값은 원본 그대로라 테마를 안 바꾸면 예전과 같은 화면이 나온다.

설정 → 외관에서 고른다. 이미 만든 보고서는 바뀌지 않는다(다시 생성해야
적용) — 그 사실을 화면에 적어 뒀다.

작업 중 잡은 것: hover 상태 3곳이 `color:#fff` 로 하드코딩돼 있어
라이트 테마에서 흰 배경에 흰 글씨가 될 뻔했다. `@media print` 블록은
인쇄용이라 의도적으로 하드코딩을 유지한다.

---

## [2.13.0] — 2026-08-19

옛 JIQT 리포트에 있던 두 기능을 **엔진 철학에 맞게 다시 만들어** 넣었다.

### 추가 — 단기 · 중기 · 장기 다지평 분석
`engine/jiqtx/horizons.py` · 63일 / 252일 / 1260일

**점수를 합치지 않는다.** 옛 리포트는 단기 55점·중기 72점·장기 74점을
기간가중해 67점(BUY)을 만들었다. 그러면 "장기 구조적 상승 안의 중기 조정"
같은 정보가 통째로 사라진다. 평균 하나만 남고, 그 평균은 어느 기간에도
해당하지 않는다.

그래서 지평별로 따로 계산하고 **어긋나는 지점을 자동 탐지**한다.
- 추세 방향이 지평 간 갈리는가
- 샤프 부호가 뒤집히는가
- 시장 베타가 지평 간 크게 다른가 (구조 변화 신호)
- 변동성 기간구조 (단기 ÷ 장기)
- 드리프트 μ̂ 와 σ/√T → 짧은 지평일수록 0과 구별되지 않음을 명시

GLD 실측: 단기 샤프 −0.52 vs 장기 +0.95 (부호 역전 탐지),
시장 베타 0.93 → 0.70 → 0.16 (붕괴 탐지), 변동성 1.35배 확대.

### 추가 — 자산군 맞춤 거시경제 대시보드
`engine/jiqtx/macro_board.py`

금에는 실질금리·달러·기대인플레가, 국채에는 명목금리·커브가, 개별주에는
시장·VIX·크레딧이 들어간다. 자산군 명세의 팩터 prior + 스트레스 축에서
변수를 고른다.

**'영향' 열을 손으로 쓰지 않는다.** `sign(추정 베타) × sign(3개월 변화)`
로 계산하고, **|t| < 2 이면 방향을 말하지 않고 중립**으로 둔다.
같은 종목·같은 날짜면 같은 표가 나온다 — 감사 가능하다.

GLD 실측: 실질금리 β −0.072(t −14.2) 3M +0.23 → **역풍**,
달러 β −1.244(t −12.0) 3M −0.55 → **지지**,
기대인플레 t +1.1 → **중립**(통계 미확인이면 말하지 않음).
전환 신호 **"달러↓ + 실질금리↓"** — 참조 리포트에 손으로 적혀 있던 문구와
같은 결론이 데이터에서 유도됐다.

작업 중 스스로 검산해 잡은 오류 3건:
- **단위 불일치** — 달러지수 베타는 로그수익률 기준인데 변화를 지수
  포인트로 재서 곱했다. 기여도가 +75.4% 로 나왔다. 베타가 추정된 단위로
  변화를 재도록 수정 (→ −1.0%).
- **베타·t 짝 어긋남** — `t_stat` 은 정적 베타의 값인데 시변 베타를 함께
  표시해 "베타 +0.004 (t=−16.6)" 같은 표가 나왔다. 같은 추정치로 통일.
- **델타 패널 컬럼명** — `tstat` 이 아니라 `t_stat`. 전부 중립으로 떨어지던 원인.

### 수정
- 다지평 계산 시 pandas Series 를 `or` 로 고르다 진리값 모호성 예외
- 거시 대시보드에서 t 가 없을 때 포맷 예외

---

## [2.12.0] — 2026-08-18

### 수정 ③ 복합 시나리오가 같은 충격을 여러 번 세던 문제
델타 패널의 베타는 **단변량**이다. "HY OAS 가 벌어질 때 이 종목이
평균적으로 얼마나 빠지는가" 에는 그때 같이 빠지는 주식시장 효과가 이미
들어 있다. 그런데 복합 시나리오("주식 −20% AND VIX +20 AND HY +300bp")
에서 이 단변량 베타들을 **그냥 더하고 있었다.** 같은 시장 충격을 3~4번
센 셈이고, 애플이 시장 −20% 에 −63.7% 빠지는(실효 베타 3배) 값이 나왔다.

그 결과 **모든 종목이 스트레스 한도(35%)를 넘겨 SIZE_ZERO 거부권**에
걸렸다. 리포트가 늘 "진입 불가" 로 끝나던 마지막 원인이다.

이제 팩터 수에 따라 베타를 구분한다.
- **단일 팩터** 시나리오 → 단변량 (총효과가 맞다. 딸려 오는 동반
  움직임까지 포함하는 게 그 충격의 실제 의미다)
- **복합** 시나리오 → **부분(다변량) 베타** (다른 팩터를 고정했을 때의 순효과)

하방 부분베타는 따로 추정하지 않으므로, 같은 팩터의
(단변량 하방 ÷ 단변량) 비율을 부분 베타에 곱해 비대칭만 옮겨 온다.
비율이 0.2~5.0 범위를 벗어나면 쓰지 않는다.

AAPL 실측:

| 시나리오 | 예전 | 수정 | 사용 베타 |
|---|---|---|---|
| 자산군 표준 충격 | −63.7% | **−25.5%** | 부분(다변량) |
| 복합: 리스크오프 | −63.7% | **−25.5%** | 부분 |
| 단일: hy_oas | −30.2% | −30.2% | 단변량 (불변) |
| 단일: mkt_excess | −23.9% | −23.9% | 단변량 (불변) |

최악 −63.7% → −30.2% → **한도 내 진입**.
판정 `NO_TRADE` → `HOLD`, 거부권 1건 → **0건**,
구속 제약이 스트레스에서 **낙폭제약 켈리**로 이동.

리포트 스트레스 표에 "베타 기준" 열을 넣어 어느 방식으로 계산했는지
보이게 했다.

### 검증 — 성격이 다른 4종목이 실제로 다른 결론에 도달한다

| 종목 | 자산군 | 아키타입 | 선택 팩터 | R² | 스트레스 | 사이즈 | 구속 | 섹션 |
|---|---|---|---|---|---|---|---|---|
| AAPL | 대형 개별주 | 우량 복리성장주 | mkt·smb·hml·umd·vix·hy | 47.9% | −30.2% | 5.0% | 낙폭제약 켈리 | 28 |
| GLD | 귀금속 | — | **실질금리·달러** | 19.5% | −16.2% | 15.0% | 자산군 상한 | 25 |
| TLT | 국채 ETF | — | **명목10년·커브·BEI·시장** | **85.3%** | −30.7% | 15.0% | 낙폭제약 켈리 | 27 |
| PFE | 대형 개별주 | 배당 인컴주 | mkt·hml·rmw·umd·vix·hy | 22.1% | −26.7% | 15.0% | 자산군 상한 | 27 |

금은 실질금리·달러로, 국채는 금리·커브로, 주식은 주식 팩터로 본다.
R² 도 자산군 기대 밴드에 맞게 갈린다(귀금속 10~55%, 국채 55~98%).
같은 대형 개별주라도 아키타입이 갈리면 활성 섹션과 사이즈 구속이 달라진다.

---

## [2.11.0] — 2026-08-18

리포트 결과가 왜 항상 ABSTAIN/NO_TRADE 로 나오는지 추적한 결과,
방법론이 아니라 **입력 데이터 두 곳**이 원인이었다. 둘 다 고쳤다.

### 수정 ① 팩터 패널이 다중공선성으로 붕괴돼 있던 문제
`PROXY_TICKERS` 가 **롱온리 ETF** 였다. 팩터는 롱숏 스프레드인데
ETF 수익률을 그대로 쓰면 6개 팩터가 전부 시장 하나가 된다.
게다가 `mkt_excess` 와 `cma` 는 **같은 티커(SPY)** 였다.

실측 (8년, SPY 대비 R²):

| 팩터 | 수정 전 | 수정 후 |
|---|---|---|
| cma | **100.0%** (SPY 그 자체) | 제거 |
| rmw | 96.6% | **0.2%** |
| hml | 85.2% | **11.3%** |
| umd | 76.8% | **1.1%** |
| smb | 75.5% | **3.2%** |

설계행렬 조건수가 특이행렬 수준 → **2.0** 으로 정상화.
`FACTOR_LEGS` 로 (롱, 숏) 다리를 정의해 스프레드를 만든다
(smb=IWM−SPY, hml=IWD−IWF, rmw=QUAL−SPY, umd=MTUM−SPY).
CMA 는 쓸 만한 무료 프록시가 없어 제외 — 없는 팩터를 SPY 로 채우느니 뺀다.

효과 (AAPL 팩터 계수):

| | 수정 전 | 수정 후 |
|---|---|---|
| mkt | 1.21 | 0.94 |
| hml(가치) | **+1.015** | **−0.293** (성장주답게 음수) |
| smb(소형) | +0.603 | **−0.085** (대형주답게 음수) |

시나리오·델타패널·스트레스·헤지비율이 전부 이 계수 위에 얹혀 있었다.

### 수정 ② 주식 아키타입이 한 번도 작동하지 않던 문제
`load_prices` 가 메타데이터를 14개 키만 통과시켜 펀더멘털이 25개 중
**6개만** 채워졌고, 그 결과 **모든 주식이 항상 '미분류'** 였다.
아키타입 9종이 준비돼 있어도 쓰이지 못했다. 화이트리스트를
`extract_fundamentals` 가 읽는 키 전부로 확대 → **25/25 확보**.

곁들여 나온 단위 버그도 고쳤다 — yfinance 는 `dividendYield` 를
퍼센트 숫자로 준다(AAPL 0.35 = 0.35%). 그런데 `> 1.0` 일 때만 100 으로
나눠서, **1% 미만 배당주가 수십 %로 읽혀** 애플·엔비디아가
'배당 인컴주'로 분류됐다. 임계를 0.25 로 낮췄다.

결과:

| 종목 | 수정 전 | 수정 후 |
|---|---|---|
| AAPL · GOOGL · NVDA | 미분류 | **우량 복리성장주** |
| F · PFE | 미분류 | **배당 인컴주** |
| TSLA · XOM | 미분류 | **경기민감주** |

### 추가 — 보고서 용어 툴팁
- `engine/jiqtx/glossary.py` — 전문 용어 **44개** 사전.
  정의만 쓰지 않고 **왜 보는지**와 **어떤 값이면 문제인지**까지 적었다.
  (PSR·DSR·PBO·Murphy resolution·과적합 갭·VaR/ES/CDaR·Kupiec·
   켈리·GARCH·FHS·GPD·팩터 R²·하방 베타·EDGE·증거 위계·생존편향 …)
- 마우스를 올리면 설명이 뜬다. 자기완결 HTML 이라 외부 라이브러리 없이
  순수 CSS + 인라인 JS. 화면 가장자리에서는 붙는 방향이 자동으로 바뀌고,
  접혀 있던 섹션을 열면 그 안의 용어도 처리된다.
- 서버에서 문자열 치환하면 HTML 속성값까지 건드려 마크업이 깨지므로,
  브라우저에서 **텍스트 노드만** 훑는다. 실측 216개 인스턴스, 깨짐 0건.

---

## [2.10.0] — 2026-08-18

**오프라인(로컬 계정) 인증 제거 — 신원은 전부 중앙 서버가 정한다.**

### 변경
- 이 PC 의 SQLite 계정 시스템을 걷어냈다. 가입·로그인·승인·어드민은
  모두 중앙 서버(Cloudflare Workers + D1)가 처리한다.
  로컬 어드민 자동 생성(무작위 비밀번호 + `ADMIN_PASSWORD.txt`)도 제거.
- **기본 서버 내장** — `version.py` 의 `DEFAULT_AUTH_SERVER`.
  설정을 하지 않으면 자동으로 이 서버를 쓴다(배포본을 받은 사람이
  주소를 몰라도 바로 로그인). 설정 → 중앙 인증에서 자기 서버로
  덮어쓰거나 `[기본값]` 버튼으로 되돌릴 수 있다.
- 프론트의 인증 이중 경로(`/api/auth/*` vs `/api/auth/remote/*`)를 하나로
  합쳤다. 로그인·가입·어드민 모두 `/api/auth/*`, `/api/admin/*` 하나만 쓴다.

### 보안
- **세션은 브라우저별로 유지한다.** 계정을 중앙으로 옮기면서
  "이 PC 에 로그인된 사람 = 요청자" 로 만들면, 외부 접근(터널) 주소를
  아는 사람이 **전부 주인 계정으로 들어온다**. 로그인 화면이 무의미해진다.
  그래서 역할을 나눴다.
  - *누구인가* → 중앙 서버 (아이디·비밀번호·승인 여부)
  - *이 브라우저가 로그인했는가* → 쿠키 (`engine/auth/session_store.py`)
  중앙 토큰은 서버가 보관하고 브라우저에는 그것을 가리키는 임의 토큰만 준다.
- 실측 확인: 쿠키 없음 → 401 · 위조 쿠키 → 미인증 ·
  유효 쿠키 → 본인 확인 후 200 · 틀린 비밀번호 → 거부.

### 추가
- `engine/auth/session_store.py` — 브라우저 세션 ↔ 중앙 토큰 (30일)
- `engine/auth/migrate_ids.py` — 예전 로컬 계정 시절의 글·분석 이력이
  새 중앙 id 를 가리키도록 username 기준 1회 재매핑
- `auth_remote`: `me_with_token` · `login_raw` · `logout_token`
  (브라우저마다 다른 토큰을 검증하려면 필요)
- QR 로그인 복구 — 비밀번호를 폰에 입력하지 않고 세션만 옮긴다

### 오프라인 정책
중앙 서버가 **명시적으로 거부**하면 즉시 세션을 버린다. 반면 그냥
**닿지 않는 것**(와이파이 끊김, 일시 장애)은 거부가 아니므로 마지막으로
확인된 신원을 24시간까지 인정한다. 분석 한 번 돌리는 중에 네트워크가
끊겼다고 앱이 잠기면 곤란하다.

---

## [2.9.0] — 2026-08-18

### 수정 (분석이 아예 안 되던 원인 3건)
- **종목 분석이 인코딩 오류로 죽던 문제.**
  `'cp949' codec can't encode character '📥'` —
  `main.py` 의 `print(f"📥 …")` 한 줄이 한국어 윈도우 콘솔(cp949)에서
  `UnicodeEncodeError` 를 내고 분석 작업 전체를 실패시켰다.
  이모지를 지우는 건 두더지잡기라 `engine/console.py` 로 출력 스트림
  자체를 UTF-8 + `errors='replace'` 로 바꾼다. 로그 정확도보다
  작업이 죽지 않는 게 중요하다.
- **모든 종목이 무결성 게이트에서 탈락하던 문제.**
  기대 거래일을 `pd.bdate_range`(월~금 전부)로 계산해, 거래소 휴장일
  9~16일/년이 그대로 "결측"으로 잡혔다. 8년이면 약 3.7% > 임계 2% →
  A1 Data Steward 거부권 → **전 모듈 무효화 → 무조건 ABSTAIN**.
  휴장일 몫을 빼고 세고, 진짜 구멍은 새로 넣은 연속공백(15일+) 검사로
  잡는다. 미국·한국·ETF·암호화폐 7종 실측 통과.
- **개별주가 채권으로 분류되던 문제.**
  키워드 검사가 `quoteType` 폴백보다 먼저 도는데, 검사 대상에
  `longBusinessSummary`(회사 사업 설명 문단)가 들어 있었다. 애플 설명의
  Apple Card 관련 'credit' 때문에 **AAPL 이 투자등급 크레딧 채권**으로
  분류됐다. 키워드는 상품명·카테고리에만 걸고, `quoteType=EQUITY` 는
  키워드보다 우선하도록 했다.
  금광(NEM)·은행(JPM)·정유(XOM) 포함 12종 분류 실측 통과.
- OHLC 정합성이 깨진 봉(소스 조정 오류)을 적재 시점에 정규화.
  종가는 건드리지 않고 고가·저가 봉투만 맞춘다 — 봉 하나 때문에
  분석 전체가 무효화되던 것을 막는다.
- 설정 헤더에 `kis` 가 그대로 노출되던 문제 (`_SET_TITLES` 누락).

### 변경
- **종목 분석과 정밀 분석을 하나로 통합.** 버튼은 `[◈ 현재 종목 분석]`
  하나이고 항상 정밀 파이프라인이 돈다. 구 `runAnalyze`/`poll`/`render`
  제거. **데모 버튼 삭제.**
- 분석 결과 화면을 실제 판정 필드로 다시 씀 — 등급·상승확률(신뢰구간)·
  배분비중·거부권/무효모듈·자산군·레짐·변동성·ML 판정·VaR·최대낙폭·
  권장사이즈·CAGR/샤프·판정근거. 애널리스트 목표주가와 분석 이력도
  같은 화면에 유지.
  (이전 코드가 `action`/`conviction`/`headline` 을 읽었는데 Verdict 에
   없는 필드라 조용히 빈 값이 나가고 있었다.)
- 정밀 분석 결과가 **분석 이력에 기록**된다 — 구 analyze 가 하던 일을 승계.
- **환영 화면**: 큰 카드 2개 → 바로가기 6개
  (차트 · 정밀 분석 · 뉴스 · 분석 이력 · 외부 접근 · 설정)
- **설정창 재구성**: 평평한 9개 → 4개 그룹
  (내 계정 / 화면 / 연결 / 기타)

---

## [2.8.1] — 2026-08-18

### 수정
- **배포된 Worker 에서 로그인이 500 으로 죽던 문제.**
  `NotSupportedError: Pbkdf2 failed: iteration counts above 100000 are
  not supported (requested 200000)` — Cloudflare Workers 의 Web Crypto 는
  PBKDF2 반복을 **10만 회로 상한** 처리한다. 플랫폼 최대값으로 낮췄다.
  - `wrangler dev --local` 은 이 제한을 강제하지 않는다. 로컬 검증만
    믿고 넘어가면 배포본에서만 터진다 — 실제로 그랬다.
    인증 관련 변경은 반드시 배포본에서 한 번 찔러 보고 끝낼 것.
  - 저장된 해시가 없는 상태(사용자 0명)에서 고쳤으므로 재설정 불필요.

### 배포
- `iaw-auth` Worker 가동 — https://iaw-auth.tenkojun.workers.dev
- D1 `iaw-auth` (01199aec…) 에 스키마 0001 · 0002 적용,
  테이블 5종(`users` `sessions` `user_pcs` `login_attempts` `audit_log`)
- 실측: `/health` 200 · 없는 계정 로그인 401 · 가입→pending 200 →
  로그인 403 · `/admin/users` 403 · `/pc/register` 401 · `/nope` 404 ·
  입력 검증 3종 400 · CORS 프리플라이트 204

---

## [2.8.0] — 2026-08-18

`.exe` 가 개발 중인 프로그램처럼 보이던 것을 정리했다.

### 변경
- **콘솔 창 제거** — `console=False`. cmd 창이 덕지덕지 뜨지 않고
  앱 창 하나만 열린다.
  - 콘솔이 없으면 `print` 한 줄에 앱이 죽을 수 있다(윈도우 빌드에서
    `sys.stdout` 이 사라진다). 그래서 **얼린 앱은 조건 없이** 표준 출력을
    `.data/logs/app.log` 로 돌린다. 창 없는 빌드에서도
    `sys.stdout.fileno()` 가 그럴듯한 값을 돌려주는 경우가 있어,
    살아 있는지 알아맞히려다 로그를 통째로 잃는다 — 실제로 그랬다.
    콘솔이 없는 앱에서 진단 수단은 이 파일뿐이다.
  - 외부 프로세스(cloudflared, taskkill)는 이미 `CREATE_NO_WINDOW` 로
    띄우고 있어 검은 창이 번쩍이지 않는다.
- **실행 파일 이름** `QuantTerminal.exe` → `Plutus.exe`
- **아이콘** — 앱의 여우 마크를 다크 원형 + 시안 링에 얹어
  `assets/app.ico` (16~256px 7종). 브라우저 탭 파비콘도 같이 생성.
- **파일 속성** — 오른쪽 클릭 → 속성에 제품명 `Plutus`,
  버전, 개발자 `Tenko jun - 정준화` 가 뜬다.
  `tools/make_version_info.py` 가 `version.py` 에서 생성하며,
  파일이 없으면 `app.spec` 이 빌드 중에 알아서 만든다.
- **배포 크기 305MB → 224MB.** 가격 캐시를 parquet → pickle 로 바꿔
  `pyarrow`(81MB) 의존을 끊었다. 로컬 재사용 캐시일 뿐이라
  컬럼형 포맷의 이점이 없다.
- excludes 정리: tensorflow · vectorbt · numba · llvmlite · shap ·
  transformers · tkinter · PyQt/PySide · jupyter

### 추가
- 런처가 **포트 충돌을 스스로 처리**한다. 8765 가 막혀 있으면 다음 빈
  포트를 찾고, 앱이 **이미 떠 있으면 서버를 새로 띄우지 않고 창만** 연다
  (중복 실행 방지).
- 창을 닫으면 터널까지 정리한다.
- `hiddenimports` 에 `engine.jiqtx.*` 25개 모듈 명시 —
  동적 임포트가 많아 PyInstaller 가 놓친다.

### 수정
- `.gitignore` 의 줄 끝 주석. gitignore 는 인라인 주석을 지원하지 않아
  패턴에 주석이 그대로 붙어 무효가 되고 있었다.

### 주의
- 빌드 후 EXE 를 한 번이라도 실행하면 `dist/Plutus/.data/` 가 생기고
  거기에 **API 키와 계정 DB** 가 들어간다. 남에게 폴더를 전달하기 전에
  반드시 지울 것. `build_windows_exe.bat` 이 빌드 끝에 자동으로 지우고
  경고도 출력한다.

---

## [2.7.0] — 2026-08-18

### 진단
외부 접근 기능을 실측해 보니 **터널 자체는 잘 뜬다** — 4초 만에 주소를
받고, 외부에서 HTTPS 로 413KB 페이지가 1.2초에 열린다. 못 쓰게 만든 건
그 다음이었다.

- cloudflared 가 죽어도 아무도 모른다 → 폰에서 갑자기 안 열린다
- 재시작할 때마다 주소가 바뀐다 → 어제 북마크한 주소는 죽은 주소다
- 앱이 비정상 종료하면 cloudflared 만 남아 떠돈다

### 추가
- **`engine/cloud/supervisor.py` — 터널 감시자.**
  - 20초마다 생사 확인, 죽으면 지수 백오프(5초→최대 5분)로 재시작
  - 주소가 바뀌면 **중앙 인증 서버에 즉시 재등록** →
    `/go/<아이디>` 고정 주소가 항상 살아 있는 터널을 가리킨다.
    사용자는 바뀌는 주소 대신 이 주소 하나만 저장하면 된다.
  - 자기가 띄운 cloudflared 의 PID 를 남겨, 다음 실행 때 유령 프로세스 정리
- `POST /api/cloud/publish` — 현재 주소 수동 재등록
- 외부 접근 패널에 감시 상태 표시 (감시 중 / 주소 발급 대기 /
  끊김 + N초 후 재시도 / 자동 복구 N회), 고정 주소 안내 블록

### 수정
- 리브랜딩 과정에서 환경변수 이름이 `JIQT_PORT` → `Plutus_PORT`
  (공백 포함)로 깨져 있던 것 → `IAW_PORT`
- 앱 종료 훅이 터널만 내리고 감시자를 남기던 문제

### 검증
- 감시자 기동 → 4초 만에 주소 발급 → `taskkill` 로 강제 종료 →
  22초 만에 자동 복구(새 주소 발급) → 정지까지 확인
- 터널 URL 외부 접근 실측: `200`, 413KB, 1.18초, `CF-Ray` 헤더 확인

---

## [2.6.0] — 2026-08-18

**중앙 인증 강화.** 내 PC 가 꺼져 있어도 가입·승인·로그인이 살아 있도록
Cloudflare Workers + D1 (둘 다 무료 티어) 위의 인증 서버를 손봤다.

### 보안 (중요)
- **하드코딩된 기본 어드민 비밀번호 제거.** 두 군데 모두.
  - Worker: `env.ADMIN_PASSWORD || 'WNSGHK'` → 시크릿이 없으면
    어드민을 아예 만들지 않는다.
  - 로컬 SQLite: `ADMIN_PASSWORD = "WNSGHK"` 상수 삭제 →
    첫 실행 시 무작위 16자를 만들어 `.data/ADMIN_PASSWORD.txt`(0600)와
    콘솔에 1회만 표시. `IAW_ADMIN_PASSWORD` 환경변수로 지정도 가능.
  - 공개 저장소에 올리는 이상, 코드에 있는 비밀번호는 비밀번호가 아니다.
- **오픈 리다이렉트 차단.** `/go/<username>` 은 사용자가 등록한 주소로
  302 를 보낸다. 검증이 없어서 누구나 임의 주소를 등록하고 이 도메인의
  신뢰를 빌릴 수 있었다. 등록 시점과 리다이렉트 시점 **양쪽에서**
  https 여부·호스트 형태를 확인하고 사설/로컬 대역을 거부한다.
- **무차별 대입 차단.** username 과 IP 를 각각 카운트, 30분 창에서
  8회 실패 시 15분 잠금. 성공하면 카운터 삭제.
- 없는 사용자로 로그인해도 해시를 한 번 돌려 **응답 시간으로 계정 존재
  여부가 새지 않게** 했다.
- 500 응답에 예외 내용을 싣지 않는다.
- 비밀번호 규칙 강화 (6자 → 8자, 숫자만 금지), username 형식 검증.

### 추가
- `POST /auth/logout_all` — 모든 기기 세션 종료
- `POST /auth/change_password` — 변경 시 다른 기기 자동 로그아웃
- `GET /auth/sessions` — 내 활성 세션 목록 (기기·발급시각)
- `GET /pc/status` · `POST /pc/unregister`
- `migrations/0002_hardening.sql` — 레이트리밋·감사 로그 테이블
- 승인/거부/비밀번호 변경 감사 로그
- 앱 측 대응 라우트 5개 + `engine/auth_remote` 클라이언트 함수

### 변경 (무료 티어 대응)
- 어드민 시드 확인을 매 요청 → **isolate 당 1회**. 요청 수만큼 발생하던
  D1 읽기가 사라진다.
- 만료 세션 청소는 로그인 20회에 1번꼴(5%)로만 실행.
- `npm run db:init` 이 0001·0002 를 함께 적용.

### 검증
- `wrangler dev --local` + 로컬 D1 로 전 흐름 확인: 어드민 시드 ·
  가입(pending) → 승인 → 로그인 · 비어드민 admin API 403 ·
  8회 실패 후 429 잠금 · 잠금 중에도 발급된 토큰은 유효 ·
  비밀번호 변경 403/200 · 오픈 리다이렉트 4종 차단, 정상 터널 URL 1종 통과

---

## [2.5.0] — 2026-08-18

### 수정
- **테마 배경 이펙트가 거의 보이지 않던 문제.** 원인이 셋이었다.
  1. `body::after` 에 그려 놓고 `z-index:0` 이라, 불투명한 패널 뒤에
     깔려 사실상 화면에 나오지 않았다.
  2. `radial-gradient` 10개를 한 덩어리로 `translateY` 해서, 흩날림이
     아니라 무늬가 통째로 미끄러지는 것처럼 보였다.
  3. 회전·흔들림·깊이감이 전혀 없었다.
- 규칙 순서 문제도 함께 정리 — 테마별 `body::after` 가 기본 규칙보다
  **앞에** 선언돼 있어 배경 그리드까지 덮어쓰고 있었다.

### 추가
- **캔버스 파티클 엔진** (`#fx`, z-index 500 · `pointer-events:none`).
  입자마다 낙하속도·흔들림 주기·회전속도·투명도가 다르고,
  작은 입자는 느리고 흐리게 그려 깊이감을 준다.
  - 🌸 SAKURA — 베지어 곡선으로 그린 실제 꽃잎. `flutter` 값에 따라
    가로폭이 줄었다 늘어나며 앞뒤로 뒤집힌다. 잎맥까지 그린다.
  - ❄ SNOW — 크기별 눈송이, 완만한 표류
  - 🍁 AUTUMN — 구르며 떨어지는 낙엽 (잎맥 포함)
  - 🌊 OCEAN — 떠오르는 기포
- 설정 → 외관에 **배경 이펙트 세기** (끄기/약하게/기본/강하게).
  입자 수는 화면 넓이에 비례해 자동 조절 (1920×1080 기준 벚꽃 82개).
- 다른 탭으로 넘어가면 자동 정지, `prefers-reduced-motion` 존중.

---

## [2.4.0] — 2026-08-18

**JIQT-X 정밀 분석 엔진 병합.** 13,366줄 단일 파일 배포판을
원래 패키지 구조(`engine/jiqtx/`, 29개 모듈)로 되돌려 붙였다.

### 추가
- `engine/jiqtx/` — 범자산 정밀 분석 엔진
  - `statcore` PSR/DSR · Purged CV · CPCV/PBO · Murphy 분해 · ACI conformal ·
    Kupiec/Christoffersen 커버리지 · DM/SPA/Hansen MCS
  - `micro` EDGE 스프레드 · Amihud · 제곱근 임팩트 · capacity 곡선
  - `vol` GJR-GARCH-t MLE 직접 구현 · HAR-RV · 언스무딩
  - `regime` Statistical Jump Model + Viterbi · 경제적 국면 명명
  - `simulate` FHS + GPD 꼬리 + 드리프트 사후분포
  - `taxonomy` 3단계 자산 분류 (메타데이터 + 통계 지문)
  - `factors` 팩터 라우터 · Kalman 시변베타 · 델타 패널
  - `equity` 9개 주식 아키타입 · 어닝/PEAD 이벤트 스터디 · 점프 · 런웨이
  - `ml` 트리플배리어 → Purged CV → 모델 경합 → **기권(abstain) 판정**
  - `options` IV 표면 · Breeden-Litzenberger RND · 그릭스
  - `risk` VaR/ES · 스트레스 · **낙폭제약 켈리**
  - `thesis`/`trade` 시나리오 · 반증조건 · 배리어확률 트레이드 · 최소분산 헤지
  - `agents`/`panel` 하드 게이트 · 전문가 14명 · 반대신문 · **결정론적 판정 엔진**
  - `charts` 외부 의존 없는 인라인 SVG 15종
  - `dynamic_report` 종목 성격에 따라 구성이 바뀌는 자기완결 HTML (35섹션 레지스트리)
  - `portfolio` 위험기여 · 팩터 넷팅 · 배분 경합(워크포워드 + Hansen MCS) · HRP
  - `ledger` 예측 저장 → 채점 → 에이전트 가중치 환류 (SQLite)
- `POST /api/jiqtx/analyze` · `GET /api/jiqtx/analyze/<job_id>` —
  백그라운드 실행 + 폴링
- 분석 위젯에 **[◈ 정밀]** 버튼 — 판정·확신도·보고서 링크 표시

### 변경
- jiqtx 의 캐시(`~/.jiqtx_cache`)와 원장(`~/.jiqtx/ledger.db`)도
  앱 폴더 안 `.data/` 로 일원화
- `requirements.txt` 에 `pyarrow` 추가 (가격 캐시 parquet)

### 검증
- 내장 검증 스위트 통과 — EDGE 스프레드 편향 없이 복원(5bp→5.4bp),
  GJR-GARCH 파라미터 복원, Murphy 판별비 411배, ACI 커버리지 오차 ≤0.5%p,
  아키타입 분류 5/5, PEAD 검출력 회수
- 오프라인 데모로 전 파이프라인 확인 — 종목당 26개 섹션 · 210KB
  **완전 자기완결 HTML**(외부 리소스 0)

---

## [2.3.0] — 2026-08-18

`JIQT` 표기를 코드·UI·문서·빌드 메타데이터 전반에서 걷어냈다.

### 변경
- 브랜드 → **Plutus**, 개발자 표기 → **Tenko jun - 정준화**
- 창 제목·페이지 타이틀·콘솔 배너·PyInstaller 스펙 모두 `version.py` 참조
- 식별자 정리: `jiqt.*` → `iaw.*` (localStorage),
  `text/jiqt-*` → `text/iaw-*` (드래그앤드롭),
  `jiqt_session` → `iaw_session` (쿠키),
  `jiqt-auth` → `iaw-auth` (D1), `jiqt-tunnel` → `iaw-tunnel`
- User-Agent `JIQT/1.0` → `Plutus/2.0`
- README 전면 재작성 (설치·키·`.data/`·구조·한계)

### 추가
- `GET /api/app/info` — 앱 이름·버전·개발자·저장소·파이썬 버전
- 설정 → 정보 패널이 하드코딩(`engine_kr v2.5`) 대신 실제 값을 표시
- 리브랜딩으로 로컬 설정이 날아가지 않도록 `jiqt.*` → `iaw.*`
  localStorage 키 1회 자동 이전 (레이아웃·관심종목·테마·드로잉)

---

## [2.2.0] — 2026-08-18

전략 백테스트 계열 기능 제거 — 이 앱은 **종목 정밀 분석**에 집중한다.

### 제거
- **STRATEGY HUB 전체** (백테스트 · BATCH · MULTI · AUTO · 리서치 ·
  vbt PRO · ML 예측 · 합성 · Paper · Tick · Kronos · 컬렉션)
- **페이퍼 트레이딩** — 주문/PnL/자동매매/리스크 한도 전부
- **리서치** — 전략 상관·민감도·워크포워드·SHAP·팩터 귀인
- **Kronos** 파운데이션 모델 연동 및 설정 패널
- 커뮤니티의 전략 첨부/가져오기
- 서버 라우트 65개, 엔진 패키지 5개
  (`strategy` `trading` `orderflow_pead` `microstructure` `backtest`),
  `report/tearsheet.py`, `static/strategy.html`
- 의존성: vectorbt · numba · llvmlite · shap · transformers ·
  huggingface_hub · einops
- 남은 사용처가 없어진 Plotly CDN 스크립트

### 수정
- **닫힌 알림 서랍의 ✕ 버튼이 허공에 떠 보이던 버그.**
  `.aw-drawer` 를 `right:-460px` 로 숨겼는데 폭은 좁은 화면에서
  `100vw` 로 바뀌어, 화면폭 461~920px 구간에서 (화면폭-460px) 만큼
  서랍이 삐져나왔다. `transform:translateX(100%)` + `visibility`
  방식으로 바꿔 폭과 무관하게 완전히 숨는다.
- **상단 📜 이력 칩이 아무 동작도 하지 않던 문제.**
  `openAnalyzeHistory()` 가 호출만 되고 정의된 적이 없었다(기존 버그).
  최근 분석 50건을 보여 주는 팝업으로 구현.

### 변경
- 작업 큐(`engine/jobs`)는 인프라만 남기고 전략 핸들러 8종 제거 —
  오래 걸리는 정밀 분석을 여기에 다시 붙인다.
- `index.html` 14,799줄 → 8,980줄 · `server.py` 3,745줄 → 2,434줄

---

## [2.1.0] — 2026-08-18

### 변경
- **외부 폴더 의존 제거.** 앱이 쓰던 `~/.jiqt/` 상태(인증 DB, API 키,
  채팅, PC 식별자, cloudflared 바이너리)를 전부 앱 폴더 안
  `.data/` 한 곳으로 모았다. 백업·이전·삭제가 폴더 하나로 끝난다.

### 추가
- `engine/paths.py` — 모든 런타임 경로의 단일 결정 지점.
  앱 폴더가 쓰기 불가면 `%LOCALAPPDATA%/i_always_win` 로 자동 강등.
  기존 `~/.jiqt` 는 첫 실행 시 자동 이전(원본은 보존).

### 수정
- `engine/__init__.py` 가 임포트 즉시 scikit-learn·torch까지 끌어오던 문제 —
  PEP 562 지연 임포트로 전환. 선택적 의존성 하나가 빠져도 패키지가
  통째로 임포트 불가가 되지 않는다.

---

## [2.0.0] — 2026-08-18

`JIQT` → **Plutus** 리브랜딩과 함께 시작하는 새 계보.

### 추가
- `version.py` — 앱 이름·버전·개발자 표기의 단일 소스
- `.gitignore` — API 키·런타임 DB·대용량 자산·빌드 산출물 배제
- `CHANGELOG.md`

### 참고
- 이 저장소에는 **API 키가 포함되지 않습니다.** 키는 앱 실행 후 설정 화면에서 입력합니다.
